****
## CONTEXT

MODULE TYPE: **EXECUTOR**
NUMBER OF VARIANTS: 3 
MODULE CONTEXT: 
- level of analysis: host level
- action type: **VM Migration (One migration)**
- number of input metrics: N/A
- selection of metrics (free to choose): N/A
	- Data structure input: single array of 2 integers `int[]`
- Data structure output: single Boolean `boolean`

FURTHER CONTEXT: Input should be an array containing the ID of the identified VM for migration, and the ID for the identified host destination.
If no migration is possible or needed, we receive `int[]{-1, -1}` as a sentinel value.
Output should be a single boolean. True for migration attempted, false for no migration attempted.
Increment a counter once per call to `execute()` where at least one action is attempted. `getActionsExecuted()` simply returns this counter.

NAMING CONVENTION: "executor_vX.java", where $X \ge 1$ 
## GUID NAMING CONVENTIONS

- Input GUID must be a string and take the form `host-migration-pair`
- No output GUID required.
## OUTPUT 
- Package all generated files into a single `.zip` archive 
- Return the `.zip` archive as the final deliverable

****
## Module Interface

```Java
public interface Executor<A> {

    boolean execute(A actions, ActionSpace actionSpace);
    int getActionsExecuted();
    String inputGuid();

}
```

## Import stub (always include)

```Java
package org.cloudbus.cloudsim.examples;

import org.cloudbus.cloudsim.Log;
import org.cloudbus.cloudsim.core.GuestEntity;
import org.cloudbus.cloudsim.core.HostEntity;
```

## Approved CloudSim API 

You may only call the following methods on CloudSim entity objects directly. 
Do not call any other CloudSim methods not listed here. 

**N/A**

****

**INITITATE GENERATION**

****