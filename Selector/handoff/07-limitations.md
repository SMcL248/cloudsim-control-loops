# 07 — Known Limitations and Methodology Lessons

## Known Limitations and Environmental Constraints

- **`actionable_cycles` is a weak, near-degenerate gate.** Saturates to the ceiling (45/45) for 3 of 5 analysers (v1/v2/v3) regardless of monitor, in 66% of all 375 rows overall. It measures "an anomaly exists somewhere," not "a migration was possible" (which needs an OVERLOADED source *and* an UNDERLOADED destination co-occurring). Only analyser_v4 and analyser_v5 show real dynamic range. `opportunityCycles` (co-occurrence-based) was added 2026-07-01 as the more meaningful denominator for conversion rate — see "Actionable-Cycle Gate" and "Instrumentation Architecture" above.
- **Self-reported module counters were an unverified trust boundary.** `getActionableCycles()`/`getActionsExecuted()` were self-tracked inside 18 independently LLM-generated modules and never independently checked. Moved to controller-tracked (`HollowedControl`) computation 2026-07-01, removing this class of risk. The old interface methods remain on the modules, unused, to avoid regeneration risk.
- **planner_v5's declared GUID input contract does not reflect its internal logic.** It gates on the declared `LoadState[]` (cpu-util-loadstate) but scores candidate migrations using independently recomputed demand pressure (`getCurrentRequestedTotalMips`), ignoring the metric that produced the classification. Confirmed from source 2026-07-01. Reframed as "GUID-agnostic design" rather than mislabelling — see planner_v5 mechanism note above — but it does violate this project's own stated rule that a planner's inputGuid should be an active semantic constraint on its internal logic, and is worth flagging as a limit on that rule's generality.
- **Module-quality conclusions can be ground-truth-metric artifacts.** planner_v5 was characterised as a "systematic failure" under the deprecated cpu-util ground truth and turned out to produce the best-in-dataset result (90.2% improvement) once the ground truth was switched to demand pressure. Its behaviour (conversion rate) never changed — only the metric used to judge the outcome did. Any per-module ranking or characterisation should be treated as provisional until cross-checked against an independent/corrected ground truth. This is now a documented methodological finding, not just a data-quality footnote.
- **Ground truth metric — resolved.** Switched from `getTotalUtilizationOfCpuMips` (scheduler-timing-noisy, structurally unbounded — confirmed via CloudSim source, see "CloudSim Observability Constraint") to `getCurrentRequestedTotalMips` (bounded ≤1.0 by construction, per `VmSchedulerTimeShared`'s no-oversubscription guarantee). New baseline: 0.222222. Old baseline (2.687) is invalidated and retained for historical reference only.
- **makespan non-discriminating** — VM migration redistributes load but cannot create MIPS. In a time-shared scheduler, cloudlets complete at the same wall-clock time regardless of migration decisions. `groundTruthAvgVariance` is the primary performance metric.
- **`isSuitableForGuest` semantic gap** — checks provisioned capacity, not runtime utilisation. Causes livelock when UNDERLOADED hosts are provisioned full. Correct planner pattern: utilisation-based destination selection first, `isSuitableForGuest` as final guard, iterate candidates on rejection.
- **`host.getAvailableMips()` does not exist** in this CloudSim build. CPU utilisation must be computed by iterating `host.getGuestList()` and summing `vm.getTotalUtilizationOfCpuMips(now)`.
- **RAM and BW signals are static** — CloudSim allocates RAM and BW statically. Excluded from the active GUID family.
- **monitor_v1 and monitor_v2 measure distinct signals** — cpu-util (`getTotalUtilizationOfCpuMips`) fluctuates with scheduling timing and can read 0.0 mid-run; cpu-demand (`getCurrentRequestedTotalMips`) is stable and deterministic. They are not API-equivalent. See CloudSim Observability Constraint above.
- **Single datacenter** — deliberate constraint for the generation experiment, not a framework limitation.
- **Executor slot has minimal variation space** — all correctly generated executors are functionally equivalent under the `int[]{vmId, hostId}` contract.
- **Module regeneration risk** — modules lost when not committed to version control. Regenerated modules under the same spec may differ from originals.
- **`vm.getCurrentRequestedTotalMips()/vm.getMips()` is a structurally degenerate ratio, not a usable demand signal.** Confirmed via diagnostic logging (2026-07-03): reads exactly `1.0` for every VM, every cycle, in the cloudlet-migration scenario — undersubscribed hosts + `VmSchedulerTimeShared`'s no-oversubscription guarantee mean any placeable VM is unconditionally granted its full rated MIPS regardless of actual cloudlet workload. Stronger case than the earlier cpu-util retirement (noisy vs. a proven hard constant). `demand` retired from the cloudlet-migration metric vocabulary; `getCurrentRequestedTotalMips()` removed from the approved API. Any Planner feasibility check built on this ratio (e.g. `planner_v1`'s `hasCapacity()`, threshold 0.85) is unconditionally unsatisfiable and will never propose a migration.
- **`MIPS_TIERS`/`PeProvisionerSimple` mismatch causes deterministic VM placement failure.** A VM MIPS tier value exceeding the per-PE provisioning cap (e.g. tier `2000` vs. `PeProvisionerSimple(1000)` with `pesNumber=1`) is structurally unplaceable — not a capacity-exhaustion failure, a single-PE cap violation for that VM's *entire* request. Deterministic given a seeded RNG (affects the same ~1/3 of the VM population every run). Fixed 2026-07-03 by lowering the offending tier to 250; any future tier changes must stay ≤ the per-PE MIPS cap.
- **`CloudletSchedulerTimeShared` has no hard rejection condition analogous to host-VM placement.** Unlike `HostEntity.isSuitableForGuest`, there is no cloudlet-migration equivalent that can reject a migration for inadequate destination-VM capacity — a VM always accepts another cloudlet, just time-shared more thinly. Any Planner "adequate resources" check is answering a question the scheduler doesn't structurally enforce; count/remaining-length-based heuristics are the correct substitute for a hard capacity gate, not a workaround for a missing one.
- **Open-ended Planner feasibility-check wording converged identically across independently generated variants.** All 5 cloudlet-migration planners implemented the same (broken) demand-ratio feasibility check from the same loosely-worded spec instruction ("must check whether destination has adequate resources"), despite independent generation. Vague constraints can produce a shared blind spot rather than genuine diversity — a methodological finding distinct from, but related to, the "GUID compatibility does not predict performance" line of results.
- **Cloudlet lookup by ID is VM-scheduler-scoped and has a destructive/non-destructive split.** `CloudletScheduler.getCloudletExecList()` is a safe, non-destructive peek; `cloudletCancel(int)` removes the cloudlet as a side effect and must not be used for resolution, since the Datacenter's own `processCloudletMove` handler performs the authoritative cancel exactly once when the migration event actually fires.
- **Cloudlet-migration family's inherited ground-truth column is structurally dead.** `average_cpu_demand_variance` (carried over unmodified from the VM-migration family) is host-level variance of per-VM demand pressure. Cloudlet migration never changes VM-to-host placement or VM MIPS ratings, so this metric cannot move under any cloudlet-migration action — confirmed 2026-07-03: identical to 15 significant figures across all 375 permutations of the corrected re-run. `makespan` is this family's actual discriminating metric (106 distinct values, 1927.04–6149.35) and matches the family's own stated goal. The ground-truth column needs replacing before it's used for anything, including cross-family/mixed-pool comparison.
- **`PowerDatacenter` with `schedulingInterval=0` hangs indefinitely rather than erroring.** Its update loop re-enters a "cancel and reschedule" branch whenever `cloudletSubmitted == CloudSim.clock()`; a 0-length interval reschedules at the identical simulated timestamp forever, so the clock never advances and the real per-cycle processing (energy accounting, cloudlet completion) is never reached. Symptom is a live, silent process producing no further output after cloudlets are submitted — not a crash, must be killed manually. Use a small positive `schedulingInterval` (e.g. 1) instead.
- **CloudSim 7.0 library bug: `HostDynamicWorkload.getCompletedVms()` flags every ordinary VM for removal after one cycle, regardless of real cloudlet completion.** Its `guest.getNumberOfGuests() == 0` check (added for 7.0's nested-container support) is unconditionally true for any VM not itself hosting containers — which is every VM in this project — so it's a false-positive removal trigger independent of actual remaining cloudlet length. Confirmed by source inspection against the `7.0` tag; not something fixable from calling code. Workaround: a narrow `PowerHost` subclass overriding only `getCompletedVms()` to drop that condition, keeping the correct `getCurrentRequestedTotalMips() == 0` check.
- **CloudSim 3.0-era documentation (the version search results default to) does not reliably describe CloudSim 7.x behaviour.** Confirmed concrete drift: entity-abstraction interfaces (`HostEntity`/`GuestEntity`/`PowerHostEntity`/`PowerGuestEntity`) are new in 7.x and don't exist in 3.0; `PowerHost.getPower(double)` changed from `protected` to `public`; `getMaxPower()`/`getEnergyLinearInterpolation()` moved from concrete `PowerHost` methods to default methods on the `PowerHostEntity` interface. Always verify against the actual pinned source (`github.com/Cloudslab/cloudsim`, matching tag) before citing specific CloudSim API details.
- **`SelectionPolicyLeastFull` (stock CloudSim 7.x) is a no-op for initial VM placement.** Its `PowerHost` branch ranks candidates by `1.0 - getUtilizationOfCpu()`, but at initial `VM_CREATE` time no cloudlet has executed anywhere yet, so every host reads exactly 0 utilization — a universal tie that a strict `>` comparison never breaks, so the policy always returns the first not-yet-excluded host in list order, reproducing First-Fit exactly. Confirmed via direct source read and a disproof trace (a genuine Worst-Fit policy could not have placed VM1 back onto Host0 given the state after VM0's placement, yet it did). Fixed locally via a custom policy (`SelectionPolicyLeastFullByCapacity`) using the class's own non-`PowerHost` branch (`getGuestScheduler().getAvailableMips()`, a static/provisioned-capacity signal) instead. The lesson generalises: utilization-based "fullness" signals are only meaningful once workload is already running; they're the wrong tool for one-time initial-placement decisions, structurally the same category of mistake as reusing a demand-based ground truth across families where it doesn't apply.
- **First-Fit placement (`VmAllocationPolicySimpler`) is a poor scenario for testing a power/consolidation objective.** Confirmed via direct arithmetic that the standard 12-VM scenario's First-Fit placement is already at or very near a local optimum for power (zero-headroom saturated host, and the one remaining host's free capacity smaller than the only VM that could conceivably move) — no feasible migration can improve on it. First-Fit was deliberately chosen to create imbalance for the QoS/load-balancing family, but "fill early hosts before touching later ones" is itself a crude consolidation heuristic, making it close to a best-case rather than worst-case starting condition for power. The power family needs the Least-Full/capacity-fix scenario (see "Hand-Coded Power-Minimizing Planner" above) to have genuine headroom to demonstrate anything.
- **`Planner5`'s single-shot destination selection caps how much consolidation gets captured, confirmed empirically not just theoretically.** With no fallback to a second-best destination when the top (most-loaded) candidate fails `isSuitableForGuest`, the planner can stall for the rest of a run even when other BALANCED hosts still have room — directly observed via before/after VM-allocation snapshots (see above). Left unfixed deliberately, as a candidate axis of variation for the LLM-generated batch (do independent generations discover a retry/fallback approach, or replicate this exact limitation — mirroring the cloudlet-migration family's "vague constraint, convergent shared blind spot" finding).
- **RAM/BW metrics are currently degenerate, not just unimplemented.** Every VM in the current harness is hardcoded to identical `ram`/`bw` values regardless of MIPS tier, so any RAM/BW-based host ratio is currently an exact proxy for VM count, carrying no independent information. Would need heterogeneous RAM/BW tiers (mirroring `MIPS_TIERS`) before being a meaningful signal — see "RAM/BW as Additional Metrics" above.
- **`vm.getCurrentRequestedTotalMips()` measures VM placement, not VM activity — confirmed from `CloudletScheduler.java` source (2026-07-07).** It sums `currentMipsShare`, set from the `mipsShare` parameter the host-level `VmScheduler` passes into `updateCloudletsProcessing()` — the allocated capacity share, not anything derived from the cloudlet exec list or `UtilizationModel`. Under `VmSchedulerTimeShared`'s no-oversubscription policy this value is granted in full the moment a VM is placed and never falls, even after every cloudlet on that VM has finished (confirmed empirically: frozen for 2400+ time units past the last cloudlet's confirmed finish time). Consequence: `UNDERLOADED`/OFF classification in the power family can only ever fire for hosts with zero VMs assigned, never for hosts holding idle-but-present VMs. See "CPU Demand Metric Is a Placement Signal, Not an Activity Signal" under Power-Aware Family.
- **`CloudletScheduler.getCurrentRequestedUtilizationOfRam()`/`getCurrentRequestedUtilizationOfBw()` do not share the CPU-demand accessor's placement/activity conflation.** Both iterate `cloudletExecList` directly and call `cl.getUtilizationOfRam/Bw(time)` per cloudlet — correctly reading 0 for idle VMs and genuinely reflecting whichever `UtilizationModel` is active. Orthogonal to the separate "RAM/BW is currently degenerate" finding above (which is about hardcoded-identical VM footprints, not this mechanism); worth revisiting if RAM/BW is ever given heterogeneous tiers, since this pathway would be a genuinely live signal where CPU demand is not. Caveat added 2026-07-07: under uniform footprints + `UtilizationModelFull`, these accessors return the concurrent cloudlet headcount wearing a RAM/BW label — deliberately excluded from the power-family approved API for that reason.
- **Instrumentation carried across families fails silently — three confirmed instances, now a standing rule.** (1) Cloudlet family: inherited demand-variance ground truth structurally dead. (2) Power family: proposal predicate tested the cloudlet family's 3-element sentinel against 2-element actions — constant-61 `actions_proposed`, zero information, live across two full sweeps before being caught. (3) Power family: inherited OVER∧UNDER opportunity gate read 0 on 48 rows that executed migrations (this family acts on BALANCED hosts). Sentinels, gates, ground truths, and counter predicates must be re-derived per family; "it compiled and produced numbers" is not evidence of transfer.
- **`compatible` logged as a single AND across three boundaries hides which link failed.** A stale executor GUID (pre-rename `host-migration-pair`) zeroed the entire compatibility axis for a full 375-row power sweep before being noticed. Log per-boundary booleans (monitor→analyser, analyser→planner, planner→executor) going forward.
- **Cross-batch GUID vocabulary drift produces systematic incompatibility when spec documents disagree.** The power family's activity metric was named `host-cpu-activity` in the monitor spec but still "VM demand" in the analyser spec at generation time; the two batches self-assembled disjoint tokens for the same metric, making every activity pairing incompatible. Root cause was spec inconsistency (confirmed), not LLM label instability — but the lesson binds either way: pin exact GUID tokens in every spec document of a family, or cross-batch composability degrades. analyser_v2/v5's GUID strings were **manually corrected post-generation** (provenance note: published power-family compatibility figures depend on these hand-edited labels; 14/125 = 11.2% compatible after the fix).
- **Illusory variant diversity: LLM variants can differ only along environmentally unreachable dimensions.** monitor_v1/v2 (power family) differ solely by a clamp to [0,1] that `VmSchedulerTimeShared`'s no-oversubscription invariant makes dead code — byte-identical behaviour across all 125 pairings, from a docstring premise ("ratio can exceed 1.0") that is false under this scheduler. Distinct from API hallucination and metric invention; detectable only by knowing the scenario's structural invariants. Variant-count claims should be discounted for such collapses.
- **The GUID convention underspecifies aggregation semantics.** monitor_v3 (sum of per-VM activity) and monitor_v4 (max per-VM activity) declare the identical `host-cpu-activity` GUID at different scales (≤1.0 vs ≤0.25 here). A GUID-matched analyser can be calibrated to the wrong scale by a label-identical monitor. Same category as the LoadState underspecification finding: the label names the metric, not its statistics.
- **Stock CloudSim 7.x `SelectionPolicyRandomSelection` is unusable for reproducible experiments.** It constructs an unseeded `RandomGen` per selection call (non-reproducible placement) and its retry loop spins forever when all candidates are excluded (silent-hang class). Replaced by `SelectionPolicyCustomRandom` (constructor-injected seeded `Random`, exclusion filter, null on exhaustion). Third stock-component defect alongside `SelectionPolicyLeastFull` (initial-placement no-op) and `HostDynamicWorkload.getCompletedVms()` (false-positive VM removal).
- **Random(seed) instances with the same seed are identical streams, not independent ones.** Two generators seeded identically produce the same state sequence, structurally correlating whatever they generate (e.g. cloudlet lengths with VM assignments). Derive sub-seeds (`seed ^ constant`) for streams with no published history; never reseed a stream whose draws are already published — the two halves of the generalised `CLOUDLET_SEED_OFFSET` lesson.
- **Result CSVs are write-once; opening them in Excel rewrites numbers.** An open-and-save converted energy values to scientific notation. Canonical result files are only ever produced by the harness; contaminated copies are regenerated, not repaired. Corollary: streaming CSVs snapshot mid-run as well-formed-but-truncated files — verify row count and last-line integrity before diagnosing crashes from an uploaded/copied file.
- **Power-model choice determines which observability signal "wins" — metric–reward alignment is a confound in module rankings.** Placement-demand monitors outperform activity monitors in the power family (~70 W) because `PowerModelLinear` reads the allocation/placement pathway, so idle-but-present VMs still draw power and activity-based OFF classification hides the cheapest evacuation candidates. Under an activity-based power model the ranking would plausibly invert. Any "monitor X beats monitor Y" claim must state the reward pathway it was scored against.
- **`vm.setMips()` alone is a complete no-op for VM MIPS scaling — confirmed via full CloudSim source trace (2026-07-10).** `Vm.getCurrentRequestedMips()` only reads `getMips()` while `isBeingInstantiated()` (t≈0); afterward it delegates to `CloudletScheduler.getCurrentRequestedMips()`, which echoes `currentMipsShare` — seeded once at initial allocation and never re-derived from `vm.getMips()` again. Any action that calls `setMips()` without also forcing `CloudletScheduler.updateCloudletsProcessing()` will show the new value in logs while producing zero effect on actual throughput.
- **The `updateCloudletsProcessing()` fix for MIPS scaling bypasses the host's own per-PE admission cap.** It writes directly into the VM's `CloudletScheduler`, never through `Host`/`VmScheduler.allocatePesForGuest()`, which is the only path enforcing `mips > peMips → reject`. Confirmed empirically: VMs born demanding MIPS above the per-PE cap are rejected outright at creation (absent from the VM Allocation log, zero cloudlets processed all run), while the same VM scaled to that identical value mid-run via the fix succeeds silently. Any MIPS-scaling result at a tier above the host's per-PE cap is exploiting this gap, not demonstrating a physically valid capacity increase. See 04-family-power.md, "VM MIPS Scaling — Built, Root-Caused, Fixed, and a Deeper Bypass Found."
- **`CloudletScheduler.updateCurrentCapacity()`'s `capacity /= Math.max(pesInUse, cpus)` means adding PEs to a VM only helps if it has multiple cloudlets genuinely concurrent.** For a VM processing cloudlets one at a time (serial), extra PEs increase both the numerator (`currentMipsShare` sum) and the denominator (`cpus`) proportionally — net zero speedup. A PE-count-based scaling action needs concurrent cloudlets on the target VM to have any effect at all.
- **In a no-migration, fixed-host-binding harness (`setDisableMigrations(true)`), makespan reduction is close to a free energy win regardless of mechanism.** All hosts stay powered (and pay their static floor) for the full makespan no matter what; any action that shortens the run — legitimate or not — directly cuts accumulated static-floor cost, which can dominate total energy deltas and mask or invert an unrelated, genuinely negative effect being tested for (confirmed: a predicted idle-inflation power cost from MIPS-scaling was real but swamped by a makespan-driven static-floor saving ~25× larger, 2026-07-10).
- **Relative (median/MAD-based) Analyser classification has no absolute anchor and degrades toward "everyone eventually flagged" as a population thins.** Finished entities reporting a zero/floor metric value drag the population median toward zero, so an entity whose own absolute metric hasn't changed can flip classification purely because its peers finished. Combined with a one-directional (escalate-only, no de-escalation) Planner, this structurally ratchets toward maximum action over a run's tail rather than targeting genuine outliers. Not a GUID-detectable failure — an Analyser can satisfy its interface contract exactly and still produce this drift; the gap is in what the spec asked for (relative standing) vs. what a well-behaved controller needs (an absolute floor, or a population-frozen-at-t0 reference, or a utilisation/efficiency framing instead of relative-to-peers). See 04-family-power.md, same section as above.
- **Quantile/top-k classification ("top 25% by metric") does not fix the relative-classification ratchet above — it inherits it and adds a fixed-quota mismatch on top (2026-07-10).** Finished entities sit at the bottom of the ranking (never in the top slice), so once fewer than the quantile fraction of the fleet is still active, every remaining active entity is trivially "in the top X%" regardless of absolute need — same population-thinning drift as median/MAD. Additionally, a fixed quota doesn't track whether the underlying distribution has one genuine outlier or several: it forces arbitrary picks among near-tied entities at the cutoff, or ignores genuinely-equal stragglers just past it. For a single-winner action budget (one action per cycle), the correct rule is a direct argmax of the raw metric among eligible entities, gated by an absolute worth-doing threshold — not a classification/quantile step at all, which only earns its keep when something downstream must still choose among a shortlist.
- **A worth-doing gate should threshold the action's actual projected payoff, not the raw triggering metric.** For VM MIPS scaling specifically: gate on `timeSaved = remainingWork × (1/currentRate − 1/nextTierRate)` exceeding roughly one MAPE cycle (`observationRate`), not on `ETC > K` for an arbitrary `K`. This correctly weights different tier jumps by what they actually buy back (a small-ratio jump and a large-ratio jump can have the same raw ETC but very different value), and it auto-suppresses near-finished entities without a separate special case, since a tiny remaining-work value produces a tiny `timeSaved` regardless of the rate ratio.
- **`Pe.status` (FREE/BUSY) is dead code under `VmSchedulerTimeShared` — confirmed via source (2026-07-14).** Neither `setStatusFree()` nor `setStatusBusy()` is called anywhere in the time-shared allocation path; `Pe.java`'s own javadoc marks both "should be used by SPACE shared hostList only." Every `Pe` reports its constructor-default `FREE` forever under time-sharing regardless of real occupancy — any code checking `pe.getStatus()` for capacity decisions is reading a field that never changes. The real per-host capacity signal under time-sharing is the aggregate `VmScheduler.availableMips`, not a per-PE flag. A PE-selection loop is also structurally unnecessary under this scheduler: `allocatePesForGuest` takes a MIPS-share list, never a specific PE id — physical-PE bin-packing happens internally via `updatePeProvisioning()`.
- **`VmSchedulerTimeShared.allocatePesForGuest` replaces a VM's entire mips-share allocation and does not credit back the VM's existing allocation before checking the new total.** Confirmed via source and reproduced empirically (2026-07-14): submitting `currentShare + oneMoreEntry` makes the internal admission check (`getAvailableMips() < totalRequestedMips`) test the VM's *whole* reconstructed allocation against current availability, not just the increment — because the VM's already-held portion was subtracted from `availableMips` when originally granted and is never returned first. Any code that "grows" an existing VM's allocation via a second `allocatePesForGuest` call (rather than an initial one) must call `deallocatePesForGuest(vm)` immediately first, or every claim beyond the VM's second PE fails deterministically once its cumulative allocation exceeds whatever's left in the host's aggregate budget — a silent, systematic failure mode that looks like normal analyser/planner retry behaviour in the logs unless the `ActionSpace` method's own success/failure is separately instrumented.
- **`requestMipsScaling`'s capacity clamp (added 2026-07-14) is narrower than the fix it looks like.** It correctly bounds every request to the VM's real per-PE ceiling (`getHostCapacity(vm)`), closing the original impossible-capacity bypass — but it still writes directly to `CloudletScheduler.updateCloudletsProcessing()` rather than through `Host`/`VmScheduler.allocatePesForGuest()`, so it never checks or decrements the host's aggregate `availableMips` budget. A VM can no longer be scaled above what a single PE could deliver, but nothing currently stops several VMs on the same host from collectively being scaled beyond what the host could deliver in aggregate. Not yet fixed; same category of gap as the one closed for PE-count scaling, just not yet ported to the rate-scaling action.
- **`actions_executed` (and any metric derived from `Executor.execute()`'s return) is attempt-level, not outcome-level, across every action type uniformly — confirmed by inspection, 2026-07-14.** `Executor.execute()` returns `true` whenever a non-sentinel action was attempted; it never queries whether the underlying `ActionSpace` call actually succeeded against real capacity, because every `ActionSpace` method is `void`. This was true before PE-count scaling existed and remains true by deliberate choice (see 04-family-power.md, "VM PE-Count Scaling," design-decision paragraph) rather than by oversight — giving one action type a genuine outcome-level success signal while every other action type and the existing `actionsExecuted` counter stay attempt-level would make "success" mean different things across action types in exactly the cross-family-comparable-metric way this project has already been burned by (see "Instrumentation carried across families fails silently" above). If outcome-level tracking is ever needed, it should be a uniform addition (post-hoc state inspection, same method for every action type) rather than a per-method signature change.
- **Comparing a controller-driven harness against a baseline harness requires matching the datacenter's own scheduling interval and utilization model, not just the controller/action logic — confirmed via a real confound, 2026-07-15.** `PowerScenarioTestControl.java`'s `PowerDatacenter` was constructed with `schedulingInterval=100` (matching the broker's own MAPE `observationRate`, an unrelated knob) against `PowerScenarioTest.java`'s `1`, and `UtilizationModelStochastic` against `UtilizationModelFull` — both silently invalidated every prior comparison between the two files. The scheduling-interval mismatch reproduces the already-documented PowerDatacenter timing-quantization mechanism (2026-07-03 entry above) applied unevenly across two files being compared rather than within one; the utilization-model mismatch is a different simulated workload, not a controller-behaviour difference. Neither produces an error — both look like a plausible energy/makespan result. Any two harnesses being compared need every simulation-fidelity knob (`schedulingInterval`, `UtilizationModel`, RNG seeds) held identical, with only the thing actually under test (the broker/controller) allowed to differ.
- **Closed-form calculations (one correct answer, no legitimate alternative strategy) should be promoted to `ReadSpace`/`ActionSpace` rather than left as LLM-generated code inside Analysers/Planners, when the research question is about strategic diversity.** Confirmed via the `getPeDemand` promotion (2026-07-15, see 04-family-power.md): a quantity like "sum of PEs demanded by a VM's active cloudlets" has no design-space variation to test — leaving it duplicated inline in every analyser only adds a place where correctness drift (hallucination) can masquerade as strategic diversity under a similarity-based diversity measure (e.g. MOSS-style scoring), since such a measure can't distinguish "genuinely different valid strategy" from "different because subtly broken" without an independent correctness oracle. General rule extending the existing `getHostCapacity`/`hostHasFreePe` precedent: facts about the world belong in `ReadSpace`, decisions belong in generated modules; if a "metric" only has one correct implementation, it isn't testing what this project's generation experiments are for.
- **A hardcoded, one-shot, time-based controller switch is a mechanism smoke test, not a switching-logic result.** The first multi-controller `Selector` test (2026-07-15, see 04-family-power.md) used `t≥1500 → second controller` purely to confirm the switch plumbing works and that composed controllers can beat single ones — not as a claim about when switching should happen. That run's own numbers show a 600-time-unit dead zone (first controller idle from t=900, switch not firing until t=1500) that a reactive rule would likely have avoided — don't cite the fixed-timestamp result as evidence about switch *timing*, only about switch *mechanism* and *composition benefit*, which are the two things it actually tested.
- **`VmSchedulerTimeShared` hard-rejects oversubscription; it does not degrade gracefully — confirmed via direct source read, 2026-07-16, correcting an assumption made earlier in the same investigation.** `allocatePesForGuest` (both the per-PE check `mips > peMips` and the aggregate check `getAvailableMips() < totalRequestedMips`) returns `false` outright on either violation; there is no partial grant or proportional slowdown of already-admitted VMs to make room. "TimeShared" (vs. `VmSchedulerSpaceShared`) refers to the ability to fractionally split a VM's MIPS requirement across multiple physical PEs (`updatePeProvisioning()`'s spill loop), not to graceful degradation under contention. Multiple VMs *can* share one host with no 1:1 VM:PE binding (confirmed empirically: 7 single-core VMs successfully co-resident on a 4-PE host) — but only because admission is gated on the aggregate MIPS sum across residents, not PE count; push that sum over the host's total and the request simply fails, it does not get a smaller share.
- **`requestMipsScaling`'s host-bypass (already flagged for exceeding a single VM's per-PE cap) has a second, more damaging consequence: the scaled value doesn't survive.** Because it writes directly to `Vm.setMips()` and `CloudletScheduler.updateCloudletsProcessing()` without ever calling `VmScheduler.allocatePesForGuest`, `VmSchedulerTimeShared`'s own `mipsMapAllocated` record is never updated to match. `PowerDatacenter`'s own regular cycle — `Host.updateCloudletsProcessing(currentTime)`, called roughly every `schedulingInterval` (1, in this scenario) — pulls `getGuestScheduler().getAllocatedMipsForGuest(vm)` straight from that stale record and pushes it back into the VM's `CloudletScheduler`, silently reverting any scale-up within about one simulated time unit of it being applied. The log shows the boost taking effect (`vm.getMips()` reads the new value at the moment it's set); the actual throughput never sustains it. Fix: route through `host.getGuestScheduler().deallocatePesForGuest(vm)` then `allocatePesForGuest(vm, newMipsShare)` (both declared on the abstract `VmScheduler` base class, confirmed via source, so this works regardless of concrete scheduler subclass), with a rollback to the original share if the reallocation fails. See 04-family-power.md, "Rate-Scaling and Load-Balancer Bug Fixes — SelectorScenario."
- **Fixing the bypass above surfaced a second, independent bug: `analyser_ETC2`'s eligibility check has no way to know a host is out of room, and re-selects the same doomed VM every cycle forever.** Its two guards (`vm.getMips() < greatestMips`, `vm.getMips() < readSpace.getHostCapacity(vm)`) only test the candidate VM's own value against two absolute, VM-local ceilings — correct only in a world where a VM's headroom to grow is bounded solely by its own per-PE ceiling, which silently assumes hosts aren't meaningfully shared. Once `VmSchedulerTimeShared` genuinely pools capacity across co-resident VMs (see above), a VM on a fully-committed host (confirmed: 8 VMs summing to exactly 4000/4000 MIPS) passes both checks indefinitely while the host has zero spare capacity to actually grant it — same infinite-retry bug class as `analyser_ETC2`'s original (07-14) and `analyser_ETC4`'s PE-deallocation bug, this time in the fixed rate-scaler's downstream consequences rather than a missing mechanical floor. Fixed with a `hostHasRoomForNextTier` guard: look up the VM's next MIPS tier, compute `host.getGuestScheduler().getAvailableMips() + (vm.getMips() * vm.getNumberOfPes())` (crediting back the VM's own current share, since it will be freed as part of the scale-up — and multiplied by PE count, since this controller rotates with the PE-scaling family on the same VM pool, so a candidate can genuinely hold more than one PE), and reject the candidate if the next tier's total demand exceeds that.
- **`Planner4` (load-balancer target-VM selection) searched the global VM list instead of scoping to the overloaded host's own residents — root cause of an observed migration-thrashing bug, 2026-07-16.** Its "find VM using most MIPS" loop iterated `readSpace.getVmList()` (every VM in the datacenter) rather than `host.getGuestList()` (only the VMs on the host currently being evaluated as overloaded), so it always selected the same globally-highest-MIPS VM regardless of which host actually tripped the overloaded threshold — observed directly as `Executor5` perpetually migrating one VM back and forth between the two most-underloaded hosts every cycle while the genuinely overloaded host was never touched. Fixed by iterating `host.getGuestList()` instead. Confirmed via re-run: correctly selects a resident of the actually-overloaded host, converges to a stable balanced state within 1-2 cycles instead of thrashing indefinitely.
- **`createVM`'s VM-generation RNG and `createCloudlet`'s cloudlet-length RNG were both literally `new Random(42)` in `SelectorScenario.java`** — the exact seed-reuse violation this project's own convention exists to prevent (05-scenarios.md, "Seed discipline"), reintroduced in the newer scenario file independent of the original `ConstructorVariableVM` instance of the same class of bug. Fixed with a derived `VM_SEED = SEED ^ 0xD1B54A32D192ED03L`.
- **Scaling multiple VM resource dimensions by the same ratio makes them mathematically collinear, not independently testable — discovered while realism-calibrating VM RAM/BW, 2026-07-17.** VM RAM/BW tiers were built by scaling both proportionally to each VM's MIPS tier (matching real general-purpose cloud instance family conventions, confirmed against real vCPU:RAM ratios), first at an arbitrary small magnitude then rescaled to match the real-world 4GB-RAM-per-vCPU standard. Both versions left the baseline scenario bit-identical to the pre-RAM/BW-tiering result. Root cause, confirmed by inspecting actual per-host VM allocation logs: every VM's RAM and BW are exact linear multiples of its MIPS (RAM = 4.096×MIPS, BW = 2.5×MIPS across all three tiers), and the host's own RAM:MIPS and BW:MIPS capacity ratios (16384/4000, 10000/4000) are the identical constants — so any VM mix that fills a host's MIPS to capacity automatically fills RAM and BW to capacity simultaneously, for any combination, not by chance of a particular random draw (confirmed directly: one host's 8 VMs summed to exactly 4000/4000 MIPS, 16384/16384 RAM, and 10000/10000 BW at once). RAM/BW admission can never independently bind under this design — MIPS will always be the (co-)binding constraint. Not a bug: this is a faithful property of real general-purpose instance family sizing, where RAM/network scale with compute specifically so they aren't the bottleneck for that workload class; real deployments only see RAM/network bind under a genuinely different (memory- or network-optimized) instance family, not variation within one general-purpose line. Any future resource-tier design that scales two dimensions by a fixed ratio should expect the same collinearity, deliberately or not. See 04-family-power.md, "RAM/BW as Additional Metrics," and 05-scenarios.md, "VM RAM/BW Footprint — Realism Pass."
- **`vm.getMips()` is a per-PE rate, not total VM throughput — a second confirmed instance of this project's per-PE-vs-total trap, found in `monitor_ETC`'s ETC formula while pre-reviewing the PE-scaling controllers for core-count heterogeneity, 2026-07-20.** `demand = totalRemainingLength / vm.getMips()` silently assumed single-PE throughput; the correct total achievable throughput across a VM's exec list is `vm.getMips() * Math.min(peDemand, vm.getNumberOfPes())`, verified against `CloudletSchedulerTimeShared`'s `getTotalCurrentAvailableMipsForCloudlet` combined with the already-documented `capacity /= Math.max(pesInUse, cpus)` formula (2026-07-14 entry below). Invisible under `pesNumber=1` everywhere, since per-PE rate and total throughput coincide with exactly one PE; would have silently biased every PE-scaling decision the moment heterogeneous core counts went live, without erroring or looking wrong in the logs. Same family as the `host.getMips()` per-PE-vs-total gotcha already documented elsewhere in this file: any metric built directly from `vm.getMips()`/`host.getMips()` without multiplying by the relevant PE count is a per-PE quantity masquerading as a total, and this project's habit of defaulting `pesNumber=1` everywhere has repeatedly hidden that distinction until something finally varies it. Fixed in `monitor_ETC.java`; see 04-family-power.md, "VM/Cloudlet Core Count — Pre-Implementation Review."
- **`requestPeAllocation`/`requestPeDeallocation` used the host's per-PE ceiling instead of the VM's own current per-PE rate for share-list bookkeeping — a second heterogeneous-core-count regression, found and fixed via the first real VM-side `CORE_TIERS` validation runs, 2026-07-20.** Both methods computed `double peMips = getHostCapacity(vm)` (the *host's* per-PE cap, 800/1000 depending on tier) and used that value to add/remove entries from the VM's own allocated-share list — but that list actually holds `vm.getMips()` (the VM's own rate) per PE, which only equals the host's ceiling by coincidence. Confirmed via a live failure: `requestPeDeallocation` on a Medium-tier VM (500 MIPS, share entries of 500) tried to `List.remove(peMips)` where `peMips` was the host's ceiling (800 or 1000) — no match, "no matching peMips entry found, aborting," repeating forever every cycle it was re-selected (same infinite-retry class as `analyser_ETC2`'s and `analyser_ETC4`'s earlier bugs). Large-tier VMs (1000 MIPS) never hit this, because they can only ever be placed on Standard/Modern hosts (Legacy's 800 cap structurally excludes them — the placement-failure risk flagged when host tiers were built), making their rate and their host's ceiling identical by construction. **Root cause of why this was invisible until now**: `analyser_ETC3`'s allocation gate requires `vm.getMips() == readSpace.getHostCapacity(vm)` before a VM is even eligible — so historically every VM that ever reached this share-list code had already been rate-scaled to the host ceiling first, making the ceiling and the VM's real rate accidentally identical every time. `analyser_ETC4` (deallocation) has no such gate, so it's the first path able to reach a VM sitting below the ceiling — which is the normal case for any Medium-tier VM that was never MIPS-scaled, and only became reachable at all once VMs could start with >1 PE. Fixed: `double peMips = vm.getMips();` in both methods. **Confirmed fixed and validated with real downstream effect, not just an absence of the error**: re-run showed the previously-failing VM deallocate successfully, plus three further Medium-tier VMs hit the same path later in the run and all succeeded; a completely separate controller (`Analyser8`, the load-balancer) that had read two hosts as permanently "overloaded" for the rest of the buggy run correctly resolved to "balanced" once the stuck PE was actually freed — the bug had been corrupting cross-controller host-state perception, not just failing a log line. Energy dropped ~2.0% between the buggy and fixed runs, consistent with several previously-stuck PEs actually being released. See 04-family-power.md, "VM Core Count — First Validated Runs."
- **`RANDOM_ASSIGNMENT`'s cloudlet-to-VM pairing is MIPS-tier-blind, and this is a real, structural source of cross-seed makespan variance — confirmed while investigating a seed-49 outlier in the multi-scenario cost validation, 2026-07-20.** `assignRng.nextInt(NUM_VMS)` picks a VM uniformly by id, with no awareness of that VM's MIPS tier, so whether a seed's heaviest cloudlet workload happens to land on a fast (1000 MIPS) or slow (250 MIPS) VM is essentially a coin flip each draw. Confirmed via direct inspection of a `remainingMI`-column VM allocation log: seed 49's unusually fast baseline (roughly half of every other seed's makespan) traced to VM#1 (fastest tier) coincidentally receiving the single heaviest workload chunk (1,269,767 MI, 7 cloudlets) while other VMs sat comparatively idle — not to an easier VM/host population (the MIPS tier mix and host-tier draw were both unremarkable). This is very likely the actual driver of the 10-scenario sweep's makespan SD (11.95%) being almost as large as its mean (12.19%) — genuine assignment-luck variance, not scenario-to-scenario population inconsistency. **Deliberate tension, not an oversight to simply fix**: uniform random assignment was chosen specifically for confound-free fairness across controller families (05-scenarios.md, "Random, for confound-free fairness" — least-full secretly favours load-balancers, first-fit secretly favours consolidation controllers). A MIPS-aware assignment policy would reduce this variance but risks reintroducing exactly that class of confound in a new place. See 04-family-power.md, "Seed 49 Investigated as a Potential Outlier."
- **Host tier heterogeneity only breaks the RAM/BW collinearity finding above where the new tier's ratio actually differs from the VM side — confirmed 2026-07-20.** Building discrete host-generation tiers (Legacy/Standard/Modern MIPS/RAM/BW, 05-scenarios.md "Host Parameter Heterogeneity") deliberately left the Standard tier at the original host constants (RAM:MIPS=4.096, BW:MIPS=2.5) for continuity — identical to the fixed VM-tier ratio. Collinearity is therefore tier-dependent, not universally resolved: it still holds locally on any Standard-tier host, and only breaks on Legacy/Modern hosts where the ratio genuinely diverges. Confirmed via the first heterogeneous run: datacenter-wide average RAM/BW utilization diverged for the first time in this project's history (39.06% vs. 38.87%), but peak RAM/BW still coincided exactly (75.00%/75.00%) — consistent with the peak-triggering host having been Standard-tier specifically. General lesson: partially heterogeneous parameter design breaks a structural-invariant finding only as much as the heterogeneity actually varies the relevant ratio — auditing "did I actually change the thing that was collinear" per tier, not just "did I add variation," is necessary before claiming a structural finding has been resolved.
- **`storageCost`'s formula double-counted VM count, inflating every reported storage/total/controllable cost figure by exactly `NUM_VMS`× (12×) from its introduction until caught, 2026-07-20.** `vmSizeMB` was already the summed total disk footprint across all VMs (the same value printed as "Total VM disk footprint"), but the formula multiplied by `NUM_VMS` again: `PRICE_PER_GB_SECOND_STORAGE * (NUM_VMS * vmSizeMB / MB_PER_GB) * makespan`. Caught not by inspection alone but by predicting the corrected value ($0.4273⁄12 ≈ $0.0356) before the fix was applied and confirming the re-run matched exactly. Same failure class as the already-documented "module-quality conclusions can be ground-truth-metric artifacts" entry above, applied to a cost formula rather than a ground-truth metric: every headline cost comparison computed before the fix (04-family-power.md, "Cost — Extended to Four Components") used a storage figure ~12× too large, which also means BW cost's share of total cost was understated in every prior write-up — corrected in place, marked not deleted, once discovered. General lesson: any aggregate computed by summing over a collection, then separately multiplied by that same collection's size, is almost certainly double-counting — worth a specific audit pass on every cost/total formula in a codebase, not just the one that happened to get caught here.
- **A datacenter-wide aggregate resource-utilization metric is structurally blind to migration — confirmed while building RAM/BW peak/average tracking, 2026-07-17.** Summing RAM/BW demand and capacity across *all* hosts before dividing (`Σ used / Σ capacity`) produced identical peak and average readings in both a no-controller baseline and a migration-performing controller run — not because no migrations occurred, but because migration only reassigns which host's tally a given VM's RAM/BW counts under. It changes neither the VM's own demand nor any host's capacity, so the datacenter-wide numerator and denominator are both mathematically invariant to migration by construction; the aggregate can only move if VMs/hosts are created or destroyed. This defeats the actual purpose of tracking peak separately from average — catching a transient, migration-driven concentration on one specific host — since a global sum cancels out exactly the redistribution effect that tracking is meant to observe. Fix: compute utilization per host each cycle and roll the *max across hosts* into the peak accumulator (and the *mean across hosts* into the average accumulator), never a whole-datacenter sum. Confirmed working post-fix: the same controller run now reads peak 75.00% vs. average 40.63% for both RAM and BW (RAM/BW identical again — the collinearity finding above, reappearing a third time), while the baseline correctly still shows peak = average, consistent with zero migrations. General lesson: any "peak vs. average" ground truth intended to catch transient per-entity extremes must be computed per-entity before aggregation, not aggregated first — the same mistake class as computing a population statistic before checking whether the thing you actually want is a property of one member, not the whole population. See 04-family-power.md, "Resource Utilization Ground Truth — Peak/Average RAM & BW Tracking."
- **`UniformDistr` owns its own internal RNG state and must not be reconstructed inside a repeatedly-called function — a third confirmed instance of this project's RNG/distribution-lifecycle trap, found while building cloudlet PE-count variation, 2026-07-20.** A first draft declared `UniformDistr coreCountDist = new UniformDistr(1, 101, CLOUDLET_PE_COUNT_SEED)` locally inside `createCloudlet()`, which is called 11 times per run (1 initial batch + 10 injections) — reconstructing it every call would have replayed the identical roll sequence every batch instead of drawing fresh values. Confirmed via direct read of `UniformDistr.java`: its `(min, max, seed)` constructor builds its own internal `UniformRealDistribution` and calls `reseedRandomGenerator(seed)` immediately in the constructor body — it owns its RNG state entirely, unlike `LognormalDistr(Random seed, shape, scale)`, which wraps an externally-supplied, already-persistent `Random` object and is genuinely safe to reconstruct per call as long as that `Random` is itself a persistent field. The two classes look interchangeable at the call site (`new SomeDistr(...).sample()`) but have opposite lifecycle requirements — confirming this is a per-class property to check via source, not a pattern that generalises across CloudSim's `distributions` package. Fixed by hoisting `coreCountDist` to a `private static final UniformDistr` field, constructed once. Same underlying bug class as the earlier `lengthRng`/`fileSizeRng`/`outputSizeRng`-as-local-variables bug (05-scenarios.md) and the `SelectionPolicy` seed-reuse bug — third confirmed instance of "an RNG or distribution object called multiple times per run must be a persistent field, not reconstructed inside the repeatedly-called function," now extended from raw `Random` objects to a `ContinuousDistribution` implementation. See 04-family-power.md, "Cloudlet PE Count — Built and Validated."
- **A cloudlet requesting more PEs than its assigned VM has is never rejected by CloudSim — confirmed via direct source read of `CloudletScheduler.updateCurrentCapacity()`, resolving the question that had blocked cloudlet-side PE-count variation, 2026-07-20.** `capacity = Σ currentMipsShare; capacity /= Math.max(pesInUse, cpus)`, where `pesInUse` sums `cl.getNumberOfPes()` across the exec list and `cpus` is the VM's real PE count — and `cloudletSubmit()` has no admission/rejection check at all. An over-requesting cloudlet is proportionally throttled down to the VM's real total capacity, never blocked; combined with `RANDOM_ASSIGNMENT` being uniform and blind to VM properties (see the seed-49 entry above), this means cloudlet PE demand can safely be drawn independently of which VM a cloudlet lands on, with no risk of a hard failure CloudSim would need to reject. Distinct from, but structurally the same category of finding as, the already-documented `VmSchedulerTimeShared.allocatePesForGuest` hard-rejection behavior (2026-07-16 entry above) — the two scheduler layers (VM-to-host PE/MIPS admission vs. cloudlet-to-VM PE-share division) have opposite failure semantics, and neither should be assumed to generalise to the other without checking source directly.
- **Host PE count is subsumed by total host capacity under `VmSchedulerTimeShared` — confirmed via direct source read, 2026-07-20, before any code was written for it.** `allocatePesForGuest`'s admission check only tests (a) each per-VM mips-share entry against `peMips` (a single scalar, the host's per-PE capacity) and (b) the VM's total requested mips against `getAvailableMips()` (the host's aggregate remaining budget) — nothing compares a VM's requested PE count against the host's actual physical PE list size. `updatePeProvisioning()`'s spill loop then packs whatever was admitted across however many physical `Pe` objects exist, moving to the next when the current one saturates; its one defensive "not enough MIPS" branch is unreachable in normal operation since the aggregate check already guarantees admitted demand can't exceed total physical capacity. Consequence: varying `pesPerHost` per tier, independent of total capacity (mips-per-PE × count), would be invisible to the scheduler — the same "illusory variant diversity" trap already caught for `monitor_v1`/`monitor_v2`. Decided not to build it; `HOST_MIPS_TIERS` already covers the one degree of freedom (total capacity) that matters.
- **`Host.isFailed()`/`setFailed()` is a real, settable CloudSim primitive that appears to be read nowhere in the actual processing path under this project's configuration — same class of finding as the `Pe.status` dead code above, discovered while pre-checking CloudSim's native failure support before scoping host failure, 2026-07-20.** Confirmed via source: `Host.java` has a genuine `failed` boolean field, and `setFailed(boolean)` marks every PE on the host FAILED via `PeList.setStatusFailed(...)`. But `VmSchedulerTimeShared.allocatePesForGuest()`/`updatePeProvisioning()` (see the host-PE-count entry above and the 2026-07-16 entry) never check `pe.getStatus()` or `host.isFailed()` anywhere in admission or provisioning. `HostEntity.isSuitableForGuest()`, the default placement-suitability check used for new VM creation, checks PE capacity/available MIPS/RAM/BW providers only — no `isFailed()` check. `PowerDatacenter.updateCloudetProcessingWithoutSchedulingFutureEventsForce()` iterates every host in `getHostList()` unconditionally each cycle for cloudlet processing and energy accrual — no `isFailed()` check either. Calling `host.setFailed(true)` alone, under this configuration, currently changes nothing observable: new VMs would still be placed there, cloudlets would keep processing normally, energy would keep accruing normally. Not fully ruled out: `VmAllocationPolicy`/`VmAllocationWithSelectionPolicy`/the project's `SelectionPolicy` classes weren't sourced this pass and could in principle gate on it separately, though the default `isSuitableForGuest()` omitting it is fairly strong evidence against. Consequence for host-failure design: `isFailed()` is a legitimate cheap fact worth exposing via `ReadSpace`, but any actual failure *consequence* (blocking new placement, evacuating existing VMs, stopping processing) has to be built as project logic — CloudSim gives none of it for free.
- **Deallocating a VM from a host does not touch the VM's own `CloudletScheduler` exec list or notify the broker — a forced host evacuation would silently stall in-flight cloudlets, not fail or return them, unless built deliberately, 2026-07-20.** `HostEntity.guestDeallocate()` calls the RAM/BW provisioners' deallocate methods and `getGuestScheduler().deallocatePesForGuest(guest)` — all host-side bookkeeping. Nothing in this path reaches into the VM's own `CloudletScheduler` (where the actual exec-list of in-progress cloudlets lives) or sends any event back to the owning broker. A cloudlet running on a VM whose host resources are pulled out from under it would simply stop making progress (zero throughput, since the VM has no allocated MIPS share) rather than being marked failed, cancelled, or returned — CloudSim has no opinion on what "the host is gone" should mean for that cloudlet's outcome. Any host-failure scenario that wants cloudlets to be genuinely lost, requeued, or migrated needs to define and implement that outcome explicitly; assuming it happens automatically would silently under-report failure impact.
- **CloudSim's own `Datacenter.processVmMigrate()` carries an unresolved, upstream-acknowledged gap in cloudlet handling during migration — a source-level `@TODO: what happens to the vmId / containerId of the cloudlets hosted on the Vm?`, noted 2026-07-20 while checking host-failure mechanics since any evacuate-then-heal strategy would lean on this exact code path.** Not investigated further this pass — flagged as a prerequisite check before building any healing strategy that migrates VMs off a failing host, since it's an open question in CloudSim's own source, not just this project's usage of it.
- **`send(host.getId(), ...)` silently misroutes when the "destination" is a plain object, not a `SimEntity` — confirmed while building host power-state control, 2026-07-27.** An early draft of `requestHostPowerDown`/`requestHostPowerUp` sent the scheduled completion event to `host.getId()`, treating `Host` as if it were independently addressable — it isn't (see "Architecture Decision: No New Interfaces Needed," 04-family-power.md: `Host`/`VmScheduler`/providers are plain Java objects owned by the `Datacenter` `SimEntity`). The correct pattern, already established for `HOST_REPAIR`/`CLOUDLET_INJECT` but not initially recognised as applying here too: `send`/`schedule` to `getId()` (the broker's own id), with the actual target's id carried as the event's *data* payload, not as the destination. Generalises: any new delayed/self-scheduled action in this codebase should default to this pattern, not to "send to the entity the action is about."
- **`actions_executed`'s attempt-level-not-outcome-level status (2026-07-14 entry above) is now only true for the *event-scheduled* action types — partially superseded, 2026-07-27.** Five `ActionSpace` methods (`requestMipsScaling`, `requestPeAllocation`, `requestPeDeallocation`, `requestRamScaling`, `requestBwScaling`) were converted from `void` to `boolean`, now returning their real, already-computed outcome instead of discarding it — see 01-architecture.md, "ActionSpace Surface — New Actions and a Return-Type Reversal." `Executor.execute()`'s own boolean, and therefore `actions_executed`, deliberately still means "fired at all" for every action type uniformly, preserving exactly the cross-action-type-comparable metric the 2026-07-14 entry was protecting — the new return values are a separate, additional, opt-in signal (`Executor.getSuccessfulActionCount()`), not a change to that metric's meaning. The 2026-07-14 entry's underlying caution (don't let one action type's outcome-level signal quietly redefine what "success" means project-wide) still fully applies; only the "every `ActionSpace` method is void" premise it was reasoned from has changed.
- **The exact bug this rewiring was trying to fix nearly reappeared inside the fix itself — `requestPeAllocation`/`requestPeDeallocation` initially returned a hardcoded `true` regardless of their own computed `success` value, 2026-07-27.** Both methods correctly used `success` to gate the actual state mutation (`if (success) { setNumberOfPes(...); }`) but then fell through to an unconditional `return true;` afterward — so a failed PE allocation/deallocation would still report success to the caller, the identical silent-misreport failure mode the boolean conversion was built to eliminate, just relocated one line down. Caught in review before being applied, not found empirically. Lesson: converting a method from `void` to a real return type is not self-verifying just because it now compiles and returns *something* — every return statement on every path needs checking against what actually happened, not just added to satisfy the compiler.
- **Indistinguishable "off" and "mid-transition" states are a reusable trap, not specific to one action — confirmed a second time via host power-up, 2026-07-27.** `isHostPoweredDown` stays `true` for the entire host-boot delay window (only clears on completion), making "off" and "currently booting" the same observable state — a second `requestHostPowerUp` call during that window passes the existing guard, queues a duplicate completion event, and the second firing's `savedPowerModels.remove(hostId)` returns `null` (already consumed by the first), leading to `setPowerModel(null)` and a live NPE risk on the next power read. Same shape as the already-documented `HOST_REPAIR`-adjacent one-action-per-cycle/statelessness caution (04-family-power.md, "Planner Design Implications") but concrete and exploitable rather than theoretical this time. Fixed with a second explicit tracking set (`poweringUpHostIds`) and a corresponding guard + `ReadSpace` read (`isHostPoweringUp`), rather than trying to overload the existing boolean. General rule for any future delayed/asynchronous action: if a precondition check and its own in-flight state are collapsed into one boolean, a second call during the delay window is a live re-entrancy bug, not just a redundant no-op — needs its own explicit "in progress" state, observable to the caller, not just internally guarded.
- **The `ReadSpace`-promotion test from 2026-07-15 ("closed-form, no legitimate alternative strategy → promote it") was too broad, and `getPeDemand` was removed as a correction, 2026-07-27.** Nearly every atomic `ReadSpace` read has exactly one correct answer — that alone doesn't distinguish "worth protecting" from "ordinary composition an LLM should be expected to do unprompted." The sharper, two-part test: promote to `ReadSpace` only when a quantity **(a)** has no legitimate strategic variation **and (b)** requires discovering something non-obvious to derive correctly from existing atomics — a hidden CloudSim relationship, a buried source-level constraint, a unit conversion nothing signals is needed. `getHostCapacity` clears both (the real per-PE ceiling lives behind `host.getGuestScheduler().getPeCapacity()`, not anywhere an LLM would guess to look). `getPeDemand` ("sum `getNumberOfPes()` over a VM's cloudlet exec list") only ever cleared (a) — summing an obvious attribute over a list the caller already has a reader for is exactly the composition this project's generation experiments are supposed to be testing, not protecting against. Distinguishes cleanly from the original "correctness drift masquerading as diversity" risk: that risk applies to computations complex enough that a wrong implementation could plausibly look like a legitimate alternative strategy to a similarity-based diversity scorer. There is no legitimate alternative way to sum PE demand — a wrong implementation reads as a bug, not disguised diversity, so the original promotion's stated risk doesn't actually apply here. Removed from `ReadSpace`/`Selector.java`; three call sites depended on it (`monitor_ETC.java`, `analyser_ETC3.java`, `analyser_ETC4.java`, all updated to inline the same sum over their own already-available cloudlet exec list) — see 04-family-power.md, "getPeDemand Removed From ReadSpace."
- **Trusting a partial code paste as if it were exhaustive produced a confidently wrong claim, caught only by going back to the actual project files, 2026-07-27.** While discussing the `getPeDemand` removal above, I (the assistant) asserted the method "isn't declared on `ReadSpace` at all right now," reasoning from every `Selector.java` paste shared earlier in the session — none of which happened to include that particular method. It was live the whole time, called from three files a direct grep of the project cache immediately surfaced. Same failure shape as this file's own "module-quality conclusions can be ground-truth-metric artifacts" and "First-Fit... crude consolidation heuristic" entries: a conclusion that looked well-supported by the evidence in front of it, wrong because the evidence itself was an incomplete sample, not because the reasoning from it was flawed. Lesson: a large file shared in pieces across a conversation should never be treated as a complete inventory of what does or doesn't exist in it — grep the real files before asserting an absence, the same discipline already applied to CloudSim API claims.
- **Several core CloudSim 7.0.1 library methods call `System.exit(0)` directly on failure conditions instead of throwing — confirmed via a full-tree `grep -rn "System.exit"`, 2026-07-28.** `Datacenter.processVmMigrate` (destination-host allocation failure), `HostEntity.addMigratingInGuest` (four separate storage/RAM/BW/MIPS capacity checks), and `PowerHost.getPower`/`PowerHostEntity.getMaxPower` (both inside a `catch(Exception e)` block) all terminate the JVM directly rather than propagating a catchable error. Because `Log.disable()` is active for the whole duration of every simulation run in this harness, the `Log.println`/`Log.printlnConcat` calls immediately preceding each `System.exit(0)` never printed either — the process death is completely silent (exit code 0, zero output), indistinguishable at a glance from a clean run ending early unless `$LASTEXITCODE`/`echo $?` is checked. No application-level `try`/`catch` can intercept `System.exit()`. Fixed locally by converting all four call sites to thrown unchecked exceptions (`IllegalStateException`/`RuntimeException`; no `throws` signature changes needed anywhere in the call chain), now caught by the harness's own `catch (Exception e)`. Any future CloudSim library method that looks like it should throw on failure is worth a quick grep for `System.exit` before assuming an uncaught exception is the worst case. See 00-INDEX.md, "Current state (2026-07-28)."
- **`mapeCycle()`'s termination condition can go permanently unsatisfied for specific Planner/Executor combinations, hanging the simulation forever in simulated time with no error and no exit — confirmed 2026-07-28.** The condition (`pendingInjections==0 && pendingFailures==0 && pendingRepairs==0 && getCloudletList().isEmpty() && getCloudletSubmittedList().size()==getCloudletReceivedList().size()`) can be left permanently false if a cloudlet is migrated or destroyed without ever being marked returned — `CloudSim.run()`'s event loop then reschedules itself indefinitely with no progress, no exception, and the process never exits. Not root-caused to a specific module combination this session; mitigated rather than fixed, with a hard watchdog ceiling (`CloudSim.terminateSimulation(END_OF_INJECTION_WINDOW * 4)`, called immediately after `CloudSim.init()`) that forces the simulation to end even if the real termination condition never fires. Distinct failure class from the `System.exit(0)` entry above: that one kills the process instantly with exit code 0, this one leaves the process alive and silently spinning — `$LASTEXITCODE` doesn't distinguish "still running" from either state, only wall-clock/progress-log staleness does. See 00-INDEX.md, "Current state (2026-07-28)."
- **A raw makespan or power extremum in a large sweep should never be cited without cross-checking secondary invariants (failure/deferral/exposure counts) against baseline first — confirmed by a real near-miss, 2026-07-28.** The single lowest-makespan row in a 10,000-row sweep (84.01 vs. baseline 10,571.17) looked like the best result in the dataset by makespan alone; checking `num_real_failures` against baseline (2, seed-guaranteed) found it read 0 for that row — the workload had been destroyed early, not completed faster, and the "win" was a measurement artifact of a controller removing cloudlets from the accounting rather than finishing them. The genuinely credible result (`analyser_v7`+`planner_v5`+`executor_v5`, −53.9% makespan / −75.9% avg power) was distinguished from this exact trap by confirming `num_real_failures`/`num_cloudlets_deferred`/`num_cloudlets_exposed` all match baseline exactly. Same failure family as this file's own "module-quality conclusions can be ground-truth-metric artifacts" entry above, but at the level of a single degenerate row rather than a metric-choice-wide bias — both are instances of "a number moved in the right direction" not being sufficient evidence on its own. See 00-INDEX.md, "Current state (2026-07-28)." **Superseded, 2026-07-29 — the "genuinely credible result" this entry cites is itself retracted; see the next four entries below. The deferred/exposed invariant check this entry recommends is not sufficient on its own — kept here as a still-useful first-pass filter, not as the final word.**
- **`num_cloudlets_deferred`/`num_cloudlets_exposed` are not reliable indicators of genuine workload loss — confirmed 2026-07-29, correcting the credibility approach used throughout the 2026-07-28 entry above.** Both are narrow, state-dependent-on-event-timing counters (deferred only increments when a *new* cloudlet's target host happens to already be down at admission time; exposed only increments for cloudlets resident on a host at the exact moment it fails) — neither one fires for the actual failure mode found this session: a controller that destroys or strands VMs through its own actions, unrelated to any host-failure event. A combination can match baseline exactly on both counters while abandoning 2–9x more cloudlets than baseline, completely undetected. The reliable signal is `numCloudletsAbandoned` (see next entry) — a field that already existed in `Selector.java` but had no getter or CSV column until this session. Any pre-2026-07-29 "credible"/"rank 1" verdict based on the deferred/exposed check alone should be treated as unvalidated, not confirmed.
- **`getNumCloudletsAbandoned()` added to `Selector.java` and wired into `SelectorMultiController`'s CSV output, 2026-07-29 — the field existed, nothing read it.** Mirrors the existing `numCloudletsDeferred` plumbing exactly (getter → `SimResult` field → populated at `runSimulation()`'s result-building step → added to the CSV header and `logResult()`'s format string/argument list). Once logged, every previously-flagged sweep result showed abandonment 2–9x baseline, including the one seed (42) that had passed the deferred/exposed check and was being treated as the clean reference result. General lesson, same shape as the `getPeDemand`/partial-code-paste entry above: an instrumentation field that exists in source but was never exposed or logged is invisible to every downstream analysis no matter how much scrutiny is applied to the numbers that *are* logged — the fix is checking what fields exist on the class, not staring harder at existing output.
- **A Planner with a one-directional, unbounded destructive action and no replenishment mechanism can depopulate the datacenter entirely over the course of a run, silently — confirmed via direct `Log.enable()` trace, 2026-07-29.** `planner_v5` (this session's "power champion") logs `"Plan destroy idle, drained VM X"` and destroys exactly one VM per MAPE cycle with no corresponding creation logic anywhere in its design. Confirmed via log: 11 VMs at t=100, 2 VMs by t=1000, 0 VMs by t=5000 (`"effective vm throughput sampled for 0 vms"`), against a baseline makespan of ~10,571 for the same seed. Every cloudlet injected after its target VM is destroyed is abandoned via `admitCloudlets`'s "was never created" path (same code path as a VM that genuinely never existed — `getGuestsCreatedList()` no longer contains it either way). The resulting −75.9% avg_power / −53.9% makespan read as a strong result on every metric checked in the 2026-07-28 sweep precisely because an empty datacenter trivially minimises both. Not a bug in the mechanical sense — `executor_v5` faithfully executes exactly what `planner_v5` asks for — but a design flaw invisible to every aggregate metric except an end-of-run VM/host count, which this session's sweep didn't track.
- **Mismatched Planner/Executor GUID pairs don't just fail silently or produce degenerate zero-effect rows — they can execute a completely different, damaging action instead, confirmed via direct log trace rather than theorized, 2026-07-29.** `planner_v8` (this session's "throughput champion," paired with `executor_v5`) logs `"Plan allocate free PE on host 1 to overloaded VM 1"` — intent: grant the VM more resources. The very next log line reads `"[executor_v5] requested destruction of vm=1"`. Every one of `planner_v8`'s real actions across the run executes as a VM destruction instead of a PE allocation, via the same "was never created" abandonment path as the `planner_v5` case above, for an unrelated reason (payload/GUID mismatch, not a design flaw — `guid_compatible=False` for this pairing, confirmed after the fact). This is the first directly-observed instance of the payload-shape-collision risk 01-architecture.md's Action Payload Encoding table flagged as theoretically possible from the start (this project's standing example being VM migration `{vmId, targetHostId}` vs. sendCloudlet `{cloudletId, datacenterId}`, both `int[2]`) — confirmation that an incompatible pairing can silently execute the *wrong* action rather than merely no-op or error. Both of the 2026-07-28 sweep's top-ranked combinations turned out to be GUID-incompatible; the sweep's own leaderboard let mismatched pairs outrank every matched one, because destroying or misdirecting VM actions trivially collapses makespan and power draw. See 00-INDEX.md, "Current state (2026-07-29)."
- **A weak-to-moderate correlation between two metrics can mean two independent mechanisms are both present, not that the relationship is weak — confirmed in hindsight, 2026-07-29.** A seed-controlled correlation between abandoned-cloudlet delta and makespan-%-change (computed before either root cause above was found) came out to r = −0.399, R² ≈ 0.16 — real but modest enough to read as "a contributing factor among several." The actual explanation, found immediately afterward via log tracing, was two structurally unrelated failure modes (destroy-with-no-replenishment; payload/GUID mismatch) both producing the same downstream symptom via different mechanisms and different timing profiles, which is exactly the kind of thing that depresses a pooled correlation coefficient without indicating a weak relationship. Don't stop at a correlation number when a mechanistic explanation (log-level tracing) is available and cheap — the correlation was useful for deciding *whether* to keep investigating, not as the final characterisation of the effect.
- **A fix ported to `Selector.java` is not automatically ported to `SelectorNoLogs.java` — confirmed as a live near-miss, not just a theoretical risk, 2026-07-29.** The `min_live_vm_count` fix required two separate additions (a hook in `requestVmDestruction`, plus a second snapshot in `processCloudletReturn` — see 00-INDEX.md, "Current state (2026-07-29), continued"); `SelectorNoLogs.java` received only the first. Caught by grepping both files side by side before the next sweep ran, not after a bad result had already been produced and analysed. `SelectorNoLogs.java` was confirmed functionally identical to `Selector.java` (bar logging) once, earlier this session — that confirmation does not carry forward across subsequent edits to either file. Any future fix to one must be explicitly checked against the other, the same discipline already applied to cross-family instrumentation ("Instrumentation carried across families fails silently," above) — this is that same failure mode at the level of two near-duplicate files instead of two families.
- **A single counter can conflate a genuine design flaw with a legitimate strategy being penalised by an unrelated infrastructure limitation — confirmed via direct code read, 2026-07-29.** `numCloudletsAbandoned` is incremented from three structurally distinct sites: `admitCloudlets()`'s "target VM was never created" branch (destruction with no replacement — `planner_v5`'s mechanism, a genuine design flaw), `admitCloudlets()`'s "host is permanently dead" branch, and `evacuateHost()`'s "no healthy host has room" branch (evacuation failure — triggered by *insufficient spare capacity elsewhere*, which a consolidation-seeking controller would trigger more often simply by leaving less slack, independent of whether it's otherwise well-behaved). All three currently read identically in the CSV and are treated identically by every validity gate built this session, meaning a controller pursuing legitimate power-saving consolidation cannot currently be distinguished from one that's genuinely destroying infrastructure. Same shape as the already-documented "compatible logged as a single AND across three boundaries hides which link failed" entry above, one level down the causal chain — an aggregate counter hiding which of several structurally different mechanisms fired. Proposed fix (not yet built): split into `numCloudletsAbandonedNeverCreated`/`numCloudletsAbandonedHostDead`/`numCloudletsAbandonedEvacuationFailed`, same wiring pattern as every other counter in this file. See 00-INDEX.md, "Current state (2026-07-29), continued," for the full proposal and the planned correlation test against `avg_ram_util`/`avg_bw_util`.
- **A single-seed result can pass every credibility and validity-gate check available and still fail to generalise, without being gamed — a second, mechanistically different reason not to cite single-seed headline numbers, confirmed 2026-07-29.** `monitor_v6`+`analyser_v7`+`planner_v8`+`executor_v7`, tested on seed 42 alone, matched baseline exactly on `completion_rate`, `num_cloudlets_abandoned`, and `min_live_vm_count` — passing every check this project's hardened validity gate applies, unlike either 2026-07-28 champion. Across seeds 42–47 it still failed the gate outright in 2/6 seeds and showed a cross-seed standard deviation *larger than its own mean* on both makespan (−15.3%±21) and power (+22%±30) change. The 2026-07-28 champions were disqualified by a hidden exploit the gate now catches; this one is disqualified by ordinary generalisation variance the gate cannot catch by construction, since it operates within one seed at a time. Confirms the project's "no headline number before multi-seed validation" convention needed to be a convention, not a shortcut that a sufficiently clean-looking single-seed result could skip — passing the validity gate is necessary but not sufficient for citing a result.
- **Disclosing an environmental risk in a module's generation context does not mean the generated module reasons about it — confirmed as an open, testable question rather than assumed either way, 2026-07-29.** The system context file given to the LLM generating the Monitor/Analyser/Planner/Executor libraries states host failure pauses processing and permanently-dead hosts have their workloads evacuated — resilience-relevant information was present at generation time, not withheld. Whether any of the 10 Analyser/Planner variants actually operationalised that information into headroom-aware behaviour (as opposed to optimising power/throughput with no regard for evacuation exposure) is not yet checked — see the `numCloudletsAbandonedEvacuationFailed` split proposed above, and the follow-up code/semantic-summary review and possible targeted regeneration experiment in 00-INDEX.md. Distinguishing "the generation context never mentioned this" from "the context mentioned it and the module didn't act on it" matters for how any resulting gap gets written up: the former is a specification gap in this project's own generation process; the latter is a finding about a limitation of LLM-generated control logic given qualitative (non-numeric-guardrail) risk information.
- **Full read of all 10 Planners and all 10 Analysers confirms zero self-imposed resource floor or fleet-health-aware classification anywhere in the library — the earlier open question is now closed, not just flagged, 2026-07-29.** No Planner sets a minimum live host/VM count or CPU-demand target range; `planner_v3`/`v4`'s `SATURATION_RATIO` constants are reactive capacity-expansion triggers, not preventive reserves, and `planner_v9`'s apparent PE floor is inherited from `ActionSpace.requestPeDeallocation`'s own mechanical minimum (2026-07-15 entry), not self-derived. No Analyser references host/VM population size, `isHostFailed`, or `isHostPermanentlyDead` at all, despite both primitives existing and being used elsewhere in the same library (`planner_v1`/`v2`) — confirming this is not a `ReadSpace` observability gap, the Analysers simply never reach for what's already available. No Analyser computes the fleet-health input a floor-aware Planner would need in the first place, so the gap holds at both MAPE stages independently.
- **`analyser_v4`'s docstring explicitly names the tradeoff it's making — "This favours rapid consolidation over headroom" — direct textual evidence against a pure capability-gap reading, 2026-07-29.** `OVERLOAD_THRESHOLD=0.60` (vs. `analyser_v1`'s otherwise-identical `0.85`) marks a host "full" and diverts placement away from it much earlier, deliberately trading headroom for faster consolidation. The concept wasn't absent from the model's reasoning during generation — it was named and weighed, then set aside because power minimisation was the sole declared goal and nothing scored the tradeoff. Refines the "capability gap" framing from earlier this session toward "goal-scoping gap": the module understood the concept it was reasoned about not respecting.
- **`planner_v2` (idle host power-down) shares `planner_v5`'s unbounded/no-floor destructive shape one layer up, and is arguably the more consequential of the two for evacuation risk specifically, 2026-07-29.** Nothing bounds how many hosts it will power down given a sustained supply of idle, drained, underloaded hosts (e.g. fed by `planner_v1`'s consolidation). Destroying a VM (`planner_v5`) doesn't remove a host from `evacuateHost`'s destination search; powering one down does, directly shrinking the exact resource that search needs. Separately, `planner_v5` was re-checked against the *other* half of the disclosed environment context (dynamic mid-sim workload arrival, not just host failure/evacuation) — it destroys any currently-idle, fully-drained VM with no reasoning about renewed demand, meaning it ignores both disclosed environmental dynamics via the same mechanism, not only the evacuation-relevant one.
- **"The LLM ignores safety checks and defaults to the most aggressive optimisation pathway" is an overgeneralisation from 2 of 10 planners, not a property of the library — corrected 2026-07-29 before it could ship in a write-up as a broader claim than the evidence supports.** `planner_v6`/`v7`/`v8`/`v9`/`v10` are mechanically self-limiting (one entity acted on per cycle, gated by an explicit threshold); `planner_v3`/`v4` are reactive capacity-*expanders*, the opposite direction from infrastructure-damaging aggression; `planner_v1`/`v2` both check `isHostFailed`/`isHostPermanentlyDead` before acting, so they respect the safety signals that do exist, just don't reserve margin against ones that haven't fired yet. The defensible claim is narrower and more specific: the runaway pattern requires pairing an *unbounded, repeatable, single-direction resource-reduction action* with *no stated stopping condition* — a design-pattern risk, not a general LLM tendency. `planner_v9` (identical missing-goal condition, structurally incapable of the runaway pattern) is the clean counterexample proving the two aren't the same claim.
- **Whether the missing resilience-awareness is a small-sample/scope problem or an underspecification problem resolved, with an explicit residual caveat, 2026-07-29.** Every one of the 20 Planner/Analyser modules' docstrings declares exactly one of two goal orientations, "(power-oriented)" or "(throughput-oriented)" — structural evidence the goal was a categorical input fixed before generation, not an emergent property that generating more variants under the same prompting would eventually diversify into a third category. Corroborates, with the first concrete empirical case, the project's own prior "maximise service" fourth-goal proposal (00-INDEX.md, 2026-07-20 entry, still unbuilt). **Caveat that must travel with this finding wherever it's cited**: the evidence supports "this library was never asked to attempt resilience-aware behaviour," not "asking would produce sound behaviour" — those are separate, and only the first is tested. See 00-INDEX.md, "Current state (2026-07-29), continued further," for the proposed regeneration experiment that would test the second.
- **A result's exact effect size can still shift meaningfully between 6 and 10 seeds even after it's already passed a multi-seed validity gate — confirmed via a real correction, not a hypothetical, 2026-07-29.** `planner_v9`+`executor_v8` passed the full validity gate at both 6 and 10 seeds, but its makespan estimate moved from an apparent free `~0%` (6 seeds) to a real `+3.2%` cost (10 seeds, std 7.5); its power estimate strengthened from `-18.6%` to `-21.6%` over the same expansion. The qualitative verdict (best-credentialed result, real, worth citing) didn't change — the exact numbers did, in a way that mattered for whether it should be described as a free win or a genuine trade-off. Passing the gate establishes a result isn't gamed or a single-seed fluke; it doesn't establish the point estimate is stable. Cite effect sizes from the largest seed count run, not the first one that clears the gate.
- **Two fully-controlled examples of the GUID compatibility signal failing in opposite directions are now on record, confirmed via the 10-seed run, 2026-07-29.** `planner_v8`+`executor_v5` (2026-07-29, earlier entry) is a true positive for the risk the GUID check exists to catch: an incompatible pairing silently executing a different, damaging action. `monitor_v9`+`planner_v9`+`executor_v8` paired with `analyser_v4` (incompatible) vs. `analyser_v7` (compatible) is the mirror case — byte-identical `completion_rate`, `num_cloudlets_abandoned`, and every other tracked metric across all 10 seeds, with GUID compatibility the only thing that differs between the two rows. Confirms the recommendation from earlier this session (don't gate exclusively on GUID compatibility, keep auditing the incompatible-but-active population) from both directions at once — the signal is real but neither necessary nor sufficient on its own, in either direction, not just usually-but-not-always reliable.
- **A GUID-mismatched Executor can inflate a completion metric by double-counting the same cloudlet, not just destroy or misdirect infrastructure — a fourth distinct mismatch mechanism, confirmed via an impossible reading rather than inferred, 2026-07-30.** `planner_v4`+`executor_v1` (GUID-incompatible) misreads `planner_v4`'s 3-element vm-creation payload as `executor_v1`'s 2-element `{cloudletId, datacenterId}` contract, so `sendCloudlet` resubmits an already-existing (often already-finished) cloudlet instead of creating a VM. Solving `completed = completion_rate × other / (1 − completion_rate)` per seed from the CSV's own accounted-for categories and comparing against baseline surfaced one seed reading `completion_rate = 1.7179` for the controller row — mathematically impossible for a bounded ratio, direct proof the numerator (`getCloudletReceivedList().size()`) exceeded what should be structurally possible. Root cause: that list is a plain `List`, not a `Set`, with no cloudlet-id deduplication anywhere in the return path, so a resubmitted cloudlet is simply appended again on its next return. Corroborating pattern across all 10 seeds: divergence from baseline scales with `actions_executed` almost exactly (0–1 mismatched actions → identical to baseline; 72–339 actions → the largest, in one case impossible, inflation). Retroactively reclassifies a chart pattern from this session's own Excel work (a high-cost/high-completion-rate cluster, previously read as a plausible "pay more, complete more" tradeoff) as this same artifact. Distinct from the three previously-documented mismatch mechanisms in this file (destruction-with-no-replenishment, payload/GUID misdirection-to-a-different-action, byte-identical false negative) — this is the first confirmed case of a mismatch inflating a metric's numerator rather than either damaging state or doing nothing. **Not yet checked**: whether `controllable_cost`/`total_cost` are contaminated the same way, if cost accrual shares `processCloudletReturn`'s code path with the list-append — flagged as the next concrete code-read, not yet done.
- **A seed-normalised metric derived directly from a compromised raw metric inherits that metric's exact vulnerability — confirmed empirically, not just reasoned about, 2026-07-30.** `completion_rate_recovery_pct` (built this session, `AverageByController.java`) is `(controller − baseline) / (1 − baseline) × 100`, seed-normalised the same way every other pct-change metric in this project's tooling is. Sorting the full 10,000-row 10-seed table by this metric unfiltered puts `planner_v4`+`executor_v1`-family mismatches at the entire top of the leaderboard — every one `guid_compatible=False`, `seeds_passing_gate=0`, the single highest value in the dataset (+84%) paired with power getting *worse* (+20%). Same standing lesson as this file's "never trust an ungated number" convention, now demonstrated for a metric that didn't exist until this session: a new derived metric must be re-checked against the gate before being used for ranking, not assumed safe because its formula is seed-normalised — normalisation protects against seed-difficulty confounds, not against a corrupted numerator.
- **A seed-normalised percentage can read far more extreme than its underlying effect because the normalising denominator is itself seed-dependent and can be near zero — confirmed while explaining a −432% reading, not a new bug, 2026-07-30.** `planner_v6`/`planner_v7`+`executor_v3` (the already-documented total-VM-pool-destruction mismatch) shows `completion_rate_mean = 0.0` across all 10 seeds — every cloudlet lost, every seed. Its `completion_rate_recovery_pct` reads −432% rather than a cleaner −100% because the formula's per-seed denominator is `1 − baseline`: in seeds where baseline's own completion rate happened to be very close to 1.0, that denominator is tiny, so collapsing to 0% divides by almost nothing and the percentage blows up into the hundreds for that one seed before being averaged in with the rest (std 513 on that row, confirming wildly different per-seed denominators, not a calculation error). General lesson for any future `X / (ceiling − baseline)`-shaped normalised metric in this project: an extreme-looking percentage should be checked against the raw underlying values before being treated as a proportionally extreme effect — the normalisation itself can be the source of the magnitude.
- **The embedded-control-character corruption first found and "fixed" 2026-07-29 recurred twice more, from three different tools, before being closed as a bug class rather than patched as a single instance — confirmed 2026-07-30.** Every attempt to write the literal six-character Java escape for a SOH control character as source text — via `Edit`, via `Write`, via a `bash`/Python heredoc — was silently converted into the actual raw control byte by the tooling itself before landing in the file, reproducing the identical corruption despite two of those three attempts being the fix for the previous occurrence (once in `AverageByController.java`'s own delimiter, again in this file's prose describing the bug). The 2026-07-29 fix addressed one occurrence, not the underlying cause — the escape sequence itself is apparently unsafe to emit through this project's tooling regardless of which tool is used. Closed by removing the mechanism rather than patching another instance: the grouping-key delimiter is now built at runtime (`monitor + (char) 1 + analyser + (char) 1 + planner + (char) 1 + executor`) instead of via any string-literal escape — ordinary Java source text with nothing for any part of the pipeline to mis-render. General lesson: if the same corruption recurs after a fix that specifically targeted it, the fix targeted the instance, not the cause — look for a way to avoid emitting the problematic pattern at all, not a more careful way to emit it.
- **`completed_naturally` was mislabeled on nearly every run, including fully successful ones — a harness bookkeeping bug, not a hung-simulation epidemic, confirmed 2026-08-03.** After the crash-log/timeout hardening pass (2026-07-28), a seed-42 rerun showed `completed_naturally=False` on 9993/10001 rows including the baseline itself — read at the time as evidence nearly every run was genuinely timing out against the watchdog. Root cause: `completedNaturally` is set only inside `mapeCycle()`'s own periodic tick, but the actual normal-termination trigger lives in `processCloudletReturn()`, which calls `finishExecution()` directly once its own (list-based) completion condition is met — ending the broker's event processing before `mapeCycle()` gets another scheduled tick to observe the same condition and set the flag. A live single-run debug trace (per-cycle Submitted/Received counts, per-cloudlet exec-list status queries added ad hoc to `Selector.java`) confirmed the seed-42 baseline genuinely finished every cloudlet around t≈10571 — matching its own historical makespan figure exactly — while still reading `completed_naturally=False`, proving mislabeling rather than a hang. Fixed by setting `completedNaturally = true` inside `processCloudletReturn()`'s own termination branch too (both `Selector.java` and `SelectorNoLogs.java` — independent files, no shared inheritance, every fix needs applying twice). Post-fix reseep (seed 42, full module library, `SelectorNoLogs`): `completed_naturally=True` jumped from 8/10001 to 9597/10001. General lesson: a status flag with two possible triggering code paths needs to be set on both, not just the one that happens to run periodically — the rare path (`mapeCycle`'s own tick occasionally catching the condition first) had been masking the common path's absence for an entire session's worth of crash-log/timeout hardening work.
- **A GUID-mismatched Executor can permanently deadlock a run by destroying a VM without resolving its in-flight cloudlets — a fifth distinct mismatch mechanism, and `executor_v5`'s third separately-documented failure mode under GUID mismatch, confirmed 2026-08-03.** After the `completed_naturally` fix above, 404/10001 seed-42 rows still read False: 113 already-tracked crashes, 291 genuine non-crash non-completions, 100% of which are `guid_compatible=False` (0/137 among GUID-compatible rows) and 205/291 (70%) involve `executor_v5`. `Selector.java`'s `requestVmDestruction` removes the VM from `getGuestsCreatedList()` and fires `VM_DESTROY` but never sweeps `vm.getCloudletScheduler().getCloudletExecList()`/`getCloudletWaitingList()` the way `evacuateHost()`'s failure branch already does for a VM that can't be evacuated — any cloudlets still on a destroyed VM are never abandoned, never returned, `cloudletsSubmitted` never reaches 0, and the run deadlocks with no exception until the watchdog. Confirmed via a direct trace on a single stuck cloudlet (`remainingLength` counting down normally, `status=INEXEC`, correctly assigned to a live VM on a host that never failed — ruling out the host-evacuation mechanism this session initially suspected) that traced back to `executor_v5` destroying its VM out from under it. This is `executor_v5`'s third separately-documented GUID-mismatch failure mode, after the 2026-07-29 un-replenished-destruction ("power champion") and payload-misdirection-to-destruction ("throughput champion") cases above — same module, three distinct exposure paths, all only surfacing under `guid_compatible=False`. Fix designed, not yet applied as of this entry: sweep and abandon both lists before removing the VM (mirroring `evacuateHost()`'s pattern), in both `Selector.java` and `SelectorNoLogs.java`. Deliberately scoped as an `ActionSpace`-level harness fix, not a patch to `executor_v5.java` itself — see next entry.
- **Whether a generated module's gap is a fair capability failure or an interface-documentation failure is a checkable, not assumed, distinction — confirmed via `executor_v5`'s destruction gap, 2026-08-03.** Before treating the gap in the previous entry as a finding about the module, checked whether `executor_v5` could have caught it using only the documented `ReadSpace`/`ActionSpace` surface it was generated against: `getVmCloudletList(GuestEntity vm)` (`Selector.java`, public `@Override`, returns the VM's exec list) already existed and was already sanctioned — `actionSpace.getVmCloudletList(vm).isEmpty()` would have caught exactly this failure mode (the stranded cloudlet was confirmed `status=INEXEC`, i.e. in that exact list). `executor_v5` already calls two adjacent methods correctly in the same block (`getVmById`, `isVmMigrating`), and its own docstring states the "only destroy idle VMs" precondition — it checks migration status but never calls `.isEmpty()` (a standard `java.util.List` method, no project-specific knowledge required) on a value it already had in hand. Because the tool was already available and already in partial use nearby, this is fairly attributed as a genuine generalisation gap in the generated code, not an interface-fairness issue the way `canMigrateGuestToHost` (2026-07-20, storage-check gap) was — that distinction determines whether the fix belongs in the spec (a new documented primitive, as `canMigrateGuestToHost` was) or is just a module that didn't use what it already had. Decided **not** to add a new `isVmIdle()`-style ReadSpace method here, since it would be redundant with `getVmCloudletList()`. One narrow residual gap noted, not acted on: `getVmCloudletList()` returns the exec list only, no exposed per-VM accessor for the waiting/queued list — irrelevant to the confirmed mechanism here (the stranded cloudlet was executing, not queued), flagged only as a latent gap if a future scenario stresses that path specifically. General lesson for future generated-code-gap findings: check whether the missing check was reachable through the already-documented interface before concluding the interface needs to grow — the two failure modes look identical from the outside (a module doing something unsafe) but call for different fixes and different write-up framing.
- **A multi-action Executor redesign (an array of GUIDs on the Planner-Executor bridge, compatible if the sets match) was proposed and set aside in favour of building out `Selector.updateSelection()` instead, 2026-07-29 — reasoning kept here since no architecture file was updated this session.** Two objections: (1) strict GUID-*set*-equality compatibility is a narrower bar than today's single-GUID scheme, not looser — it requires the executor to replicate the planner's exact action repertoire, likely reducing compatible pairs rather than increasing design-space exploration as intended; a subset rule (planner's GUID set ⊆ executor's) would fix that but requires per-action GUID tagging rather than a fixed ordinal-position array, since a variable-length subset breaks positional decoding. (2) A Planner that can emit any of several action types depending on internal branching reintroduces, deliberately this time, the exact multi-mechanism-in-one-class opacity this session's two retracted champions came from (`planner_v5`'s single hidden mechanism, `planner_v8`/`executor_v5`'s payload reinterpretation) — every finding from a full-file read this session depended on one Planner having exactly one attributable strategy. `Planner.outputGuid()` being fixed per class already structurally prevents any single generated Planner from combining mechanisms — composing already-validated single-mechanism controllers via the Selector's long-parked `updateSelection()` hook is the lower-risk path to a dual winner, reusing debugged components instead of generating new, harder-to-interpret ones. Not built either way this session; flagged as the next concrete step if a dual winner remains the goal.
- **A numeric-bounds check is not an identity check, and the gap between them let a misdirected event silently terminate the entire simulation — sixth catalogued GUID-mismatch mechanism, confirmed 2026-08-04.** `Selector.sendCloudlet`'s original guard (`datacenterId < 0 || datacenterId >= CloudSim.getNumEntities()`) only rejected out-of-range ids; any in-range-but-wrong id passed straight through, including CloudSim's own internal `CloudSimShutdown` entity (always id 0 in this harness). `CloudSimShutdown` calls `CloudSim.abruptallyTerminate()` — sets a flag checked only between full clock ticks — the instant it receives its `numUser`-th event, and this harness constructs `CloudSim.init` with `numUser=1`, so a single misdirected event ends the run with no exception, no log, no watchdog trip (the tick that triggers it completes fully first, so `mapeCycle()`'s own `schedule()` call for the next tick can succeed and still never fire). Traced from symptom (unconditional CHECKPOINT prints in `mapeCycle()` showing `schedule()` succeeding but the next tick never arriving) to root cause via direct source reads of `CloudSim.run()`/`runClockTick()` and `CloudSimShutdown.java`, confirmed with a one-off `CloudSim.getEntityName(datacenterId)` print showing the actual misdirected target. Fixed: `sendCloudlet` now checks `getDatacenterIdsList().contains(datacenterId)`, a genuine identity check against registered datacenters, matching the fix pattern already used elsewhere in this project rather than a wider bounds check. `executor_v1`'s unchecked raw `datacenterId` forwarding is the proximate trigger — same root defect (no validation of a forwarded raw id) as the already-documented 2026-07-30 cloudlet-double-counting bug, a second distinct failure mode from the same design choice. See 00-INDEX.md, "Current state (2026-08-04)."
- **`requestPeAllocation`/`requestPeDeallocation` permanently strip a VM's host-level PE allocation on a failed reallocation, with no rollback — a real harness bug independent of any GUID mismatch, confirmed 2026-08-04.** Both methods call `host.getGuestScheduler().deallocatePesForGuest(vm)` then `allocatePesForGuest(vm, newShare)` with no rollback branch if the second call fails a hard capacity check — unlike `requestMipsScaling`, immediately above both in the same file, which already restores the VM's original share on failure (added 2026-07-14/16). Confirmed via CloudSim's own `VmSchedulerTimeShared.java` source: `deallocatePesForGuest(guest)` removes the guest from `mipsMapRequested`/`mipsMapAllocated`, resets available capacity, then re-derives every *other* resident VM's allocation from their existing requested shares — safe for those VMs, but the calling method's own subsequent `allocatePesForGuest(vm, newShare)` call can still fail its capacity check, and on failure leaves `vm` permanently absent from `mipsMapAllocated` (the map `getAllocatedMipsForGuest()` reads). Every later call reads `null` forever after, silently, with no exception — any executor gated on that value (confirmed via `executor_v7`'s repeated `requestPeAllocation` calls) deadlocks permanently with no error, indistinguishable in the logs from a genuinely-stuck cloudlet until traced. Confirmed real (not the earlier-session false alarm) via `remainingLength` frozen bit-identical across 65+ consecutive cycles, vs. the earlier case's visibly decreasing values. Confirmed host- and executor-neutral: this would corrupt any VM on any host whenever capacity runs out, regardless of which action triggered the deallocate. Fixed by porting `requestMipsScaling`'s existing rollback pattern to both methods (restore `currentShare` via `allocatePesForGuest(vm, currentShare)` on failure, then return `false`) — applied to both `Selector.java`/`SelectorNoLogs.java`, confirmed via re-run that the previously-frozen `executor_v7` cluster now completes normally. General lesson, same shape as the already-documented `allocatePesForGuest`-doesn't-credit-back entry above: a fix ported to one method in a file is not automatically ported to structurally identical methods in the same file — `requestPeAllocation`/`requestPeDeallocation` had the exact same deallocate-then-reallocate shape as `requestMipsScaling` for weeks without receiving the same rollback fix. See 00-INDEX.md, "Current state (2026-08-04)."
- **`requestCloudletPause` has no structural path back to `requestCloudletResume` under the current single-`ControlUnit`-per-run architecture — a new deadlock risk identified by design review, not yet triggered by anything in the library, 2026-08-04.** `Selector.updateSelection()` is still an empty, deferred hook, so exactly one `ControlUnit` (one Monitor+Analyser+Planner+Executor combination) runs per simulation. A Planner/Executor pair whose declared action is "pause" has no way to later resume the same cloudlet within that run — resuming it requires a *different* Planner/Executor pair (a different GUID, a different generated Executor), which cannot co-run under the present single-selection architecture. A cloudlet paused this way is not counted anywhere: it isn't cancelled (so it never increments any abandonment counter), it just sits in `getCloudletSubmittedList()` with `remainingLength` frozen, silently blocking `mapeCycle()`'s natural-completion condition — the identical failure signature to the already-fixed `executor_v7`/PE-allocation deadlock (entry above), just via a different mechanism and not yet exercised by any generated module. Consequence, not yet executed: `requestCloudletPause`/`requestCloudletResume` are being dropped from the `ActionSpace` surface advertised to future generation batches (kept implemented in the real interface, not deleted) — see 00-INDEX.md, "Current state (2026-08-04), continued."
- **A GUID-compatible pairing can disagree on what the shared value's units even are, and still run without error — a third concrete instance of this pattern, found by inspection this time rather than a live trace, 2026-08-04.** `monitor_v2` outputs raw host MIPS utilization (`usedMips / totalMips`, self-labelled `"host-util-mips"`); `analyser_v2`'s own docstring and `inputSemantic()` (`"host-power-draw-watts"`) assume it's receiving power draw in Watts. Both declare GUID 1200 and the pairing runs cleanly — `analyser_v2`'s classification is IQR-based (relative to the observed distribution each cycle), so it's unit-agnostic and still produces a coherent OVERLOADED/UNDERLOADED/BALANCED split, just not the one its own docstring describes. Complements the two mismatch-signal-reliability cases already on record (`planner_v8`+`executor_v5` true positive; `monitor_v9`+`planner_v9`+`executor_v8`-with-`analyser_v4` false positive) with a third shape: a pairing that's GUID-compatible, runs without error, and produces plausible-looking output, while the two modules privately disagree about what that output means. Reinforces the standing recommendation to add explicit "GUID matching is mechanical, not semantic" language to the module-generation system context — drafted this session, not yet applied.
- **A newly-added counter can exist, be correctly incremented, and still be invisible to the one formula that was supposed to use it — the `getPeDemand`/`numCloudletsAbandoned` "instrumentation exists but nothing reads it" trap recurring in a new shape, caught before a sweep ran rather than after, 2026-08-04.** While wiring `numCloudletsAbandonedHostPoweredDown` through `SelectorMultiController.java`, the counter itself was correctly populated in `SimResult` and correctly printed to the CSV — but `completionRate`'s `totalAccountedFor` denominator (`newList.size() + numCloudletsAbandoned + cloudletsStillInFlight + numCloudletsStillDeferred`) was never updated to include it. A controller powering down loaded hosts would have had every cloudlet it destroyed vanish from the completion-rate accounting entirely — not completed, not abandoned (in the formula's eyes), not in-flight, not deferred — inflating `completion_rate` rather than depressing it, the exact opposite of the intended effect and precisely the exploit this whole change was built to expose. Caught by manual review of the formula before any sweep was run with it live, not by a bad result surfacing later. General lesson, same shape as the `getPeDemand`-removal and `numCloudletsAbandoned`-had-no-getter entries above: a new field being correctly wired into the CSV output does not guarantee it's been wired into every *derived* metric that logically depends on it — each downstream formula needs an explicit check, not just the raw counter.
- **`planner_v2`/`executor_v9`'s zero-successful-actions result from the 2026-07-28 full sweep (flagged then as not-yet-root-caused, alongside `planner_v3`/`executor_v10`) is very plausibly explained, not a mystery, 2026-08-04.** Direct source read of both modules shows two independent, redundant self-guards: `planner_v2` skips any host where `!guests.isEmpty()` before proposing power-down; `executor_v9` separately refuses to act if `vmsOnHost` is non-empty, checking the identical condition again. Both checks predate and are entirely independent of the `ActionSpace`-level guard that used to live in `requestHostPowerDown` itself. With 12 VMs spread across 4 hosts and continuous cloudlet injection for the whole scenario window, a host that is simultaneously idle and fully drained of every VM is a narrow, rarely-satisfied precondition under this harness's default population — not a bug in either module, just a tight condition real workload seldom produces. Not independently confirmed for `planner_v3`/`executor_v10` this session (not read), but the same explanation is a reasonable first hypothesis given the shared symptom. Worth a formal write-up closing this out as resolved rather than open, per the discipline this file follows elsewhere (mark as resolved, don't leave it silently stale).
- **`executor_v1` (the `sendCloudlet` action) is being retired from the module library, not fixed further — a scope decision, not a technical finding, 2026-08-04.** Motivated by convergence of evidence across two sessions: the 2026-07-30 cloudlet-double-counting bug and this session's `CloudSimShutdown`-misdirection bug are both direct consequences of the same design choice (`executor_v1` forwards raw `cloudletId`/`datacenterId` values with no validation), and 21 of the 32 deadlocks remaining after this session's fixes still involve `executor_v1`, unchanged in count from before those fixes — i.e. a third, still-unexplained failure mode, not one either fix touches. Decided against a further targeted fix (e.g. adding a `numInvalidActionsAttempted` counter, considered and declined this session) on the reasoning that raw-id-forwarding actions like this one are expected to become defunct in favour of a future "traffic director" action family — not yet in scope, so further hardening of a soon-to-be-retired action isn't worth the investment. Action item, not yet executed: remove from `SelectorMultiController`'s `executorLib`, check for any Planner left with no matching Executor as a result, re-sweep. See 00-INDEX.md, "Current state (2026-08-04)."
- **An array-*length* mismatch between an analyser's output and a planner's declared input contract is a distinct GUID-mismatch shape from every value/payload-shape collision catalogued so far — confirmed via `planner_v10`/`monitor_v4`/`analyser_v1`, 2026-08-04.** Every prior mismatch mechanism in this file involves a fixed-size payload being misread with the wrong values (`{cloudletId, datacenterId}` read as `{vmId, hostId}`, etc.) — same array length, wrong interpretation. This one is different: `monitor_v4`/`analyser_v1` emit a genuinely well-formed `LoadState[]`, but sized to the (fixed, always-4) *host* count, while `planner_v10` requires it sized to the (variable, workload-dependent) *live cloudlet* count, per its own explicit guard (`diagnosis.length != activeCloudlets.size()`). Confirmed via a live trace showing the pipeline reduced to a permanent no-op once cloudlet count diverges from 4: `"[planner_v10] Diagnosis/cloudlet size mismatch. No-op."` every cycle, returning the `{-1,-1,-1}` sentinel, which `executor_v2` correctly reads as a source==destination skip. `planner_v10`'s own declared `outputGuid` (3003, `cloudlet-move`) genuinely matches `executor_v2`'s `inputGuid` (3003, `moveCloudlet`) — confirmed by direct source read of both, the *executor* link is not the incompatible one here; the mismatch is one boundary earlier, at monitor/analyser→planner, consistent with the already-documented "`compatible` logged as a single AND across three boundaries hides which link failed" entry. Not fixed and not attributed as a harness bug — this is exactly the kind of GUID-incompatible garbage the sweep is designed to observe, now with one more confirmed mechanism shape in the catalog.
- **Decision, stated directly by the project owner, 2026-08-04: stop root-causing individual remaining timeout/deadlock rows and discard them from analysis instead, now that they're a small enough fraction of the sweep.** After this session's four fixes, non-crash deadlocks fell from 91/10,001 to 32/10,001 (0.32%), and every mechanism investigated this session (the `CloudSimShutdown` misdirection, the PE-allocation rollback bug, the `planner_v10` size-mismatch case) confirmed as either a genuine GUID-mismatch consequence or an already-fixed harness defect, never a case where the harness itself was corrupting the measurement silently. Judged not worth continuing to chase the remaining residual to zero. This is a reporting-time filter (exclude non-`completed_naturally` rows before drawing conclusions from a sweep), not a harness or module change — consistent with the standing project convention that GUID-mismatch consequences are left observable rather than suppressed; the decision is about which rows get *analysed*, not about hiding or fixing what happens during simulation. See 00-INDEX.md, "Current state (2026-08-04)."
- **Cloudlets are bound to a VM ID at cloudlet-creation time, before VM creation outcomes are resolvable, so a cloudlet whose target VM never gets created is permanently stranded and never counted anywhere — a real bug, not the `CONTENTION_LOAD` lever's intended effect (which is genuine VM-creation failure itself), confirmed and fixed 2026-08-11.** `createCloudlet` assigns each cloudlet a random `guestId` via `assignRng.nextInt(NUM_VMS)` before any VM has actually been created — `NUM_VMS` is a request count, not a guarantee. Confirmed empirically via `results_lever.csv`'s baseline row: `completion_rate=0.9500`, `num_cloudlets_abandoned_vm_never_created=3`, `min_live_vm_count=11` vs. `NUM_VMS=12` at `CONTENTION_LOAD=0` — even the no-failure baseline never reached a clean 1.0 completion rate. Fixed in both `admitCloudlets` and `sweepAbandonedCloudlets` (`Selector.java`, confirmed applied by the project owner; `SelectorNoLogs.java` port **not confirmed**, needs an explicit check): re-roll the orphaned cloudlet's `guestId` onto a random member of `getGuestsCreatedList()` via the already-seeded `unrecoverableRng`, then fall through into the *existing* `isHostFailed`/`isHostPermanentlyDead` branch logic rather than bypassing it. **A first draft of the `admitCloudlets` fix added the reassigned cloudlet straight to the admissible list without re-checking the new target's host health — caught specifically because the project owner asked "does this set include FAILED, and DEAD [hosts]?"**, which prompted confirming via direct source read of `DatacenterBroker.java` and `Selector.java` that `getGuestsCreatedList()` is never filtered by host health anywhere in this codebase (only mutated by creation-ack and explicit destruction) — general lesson matching several entries above: a plausible-sounding assumption about what a list "obviously" contains is exactly the kind of thing this project has repeatedly needed to verify via source rather than infer. All-hosts-down edge case checked and confirmed to route through the existing defer/`HostDead`/evacuation-failure mechanisms rather than introducing a new, uncounted loss category.
- **A Monitor historical-data-access capability was checked empirically before being designed, rather than assumed necessary or assumed absent — confirmed 0/10 of the current `monitor_v1`-`v10` batch self-implements any form of cross-cycle state, despite nothing in the interface preventing it, 2026-08-11.** Every field declared across all ten files is `private static final` (constants: epsilon values, sentinels, GUIDs, semantic strings) — no `List`/array/mutable instance field anywhere, meaning no monitor could carry information between calls even if its logic wanted to. Two readings left open, not resolved: either the interface shape (`D analyse(M metrics, ReadSpace readSpace)`, a fresh array every call) never nudges the LLM toward temporal reasoning and explicit `ReadSpace` support is the only way this project will ever observe it, or 10/10 convergence on stateless snapshot classification is itself a genuine, citable finding about what emerges from the task as specified rather than a scaffolding gap. Full design for the capability (which of Selector's 18 already-tracked per-cycle reading lists to expose, a consolidated `getMetricHistory(MetricType)` accessor recommended over 18 bespoke getters to avoid roughly doubling `ReadSpace`'s method count, and a necessary GUID amendment repurposing the previously-fixed final digit of the `1_X_00`/`2_X_00` GUID shapes into a per-entity-vs-per-cycle historical flag) is written up in 00-INDEX.md, "Current state (2026-08-11)" and 05-scenarios.md — **not actioned this session**, explicitly deferred as a cost/benefit call (reopening a finalized spec/generation pipeline) rather than a technical blocker. **Decided, 2026-08-11: no.** The project owner will not pursue historical-data exposure — the workflow is now treated as finalized ("well defined") rather than still being iterated on, and future work uses the current/most-recent module generation as-is rather than reopening the interface. This closes the item: "0/10 of the current monitor batch self-implements history despite being free to" stands as the reportable finding on its own (workflow-as-specified produces stateless snapshot classification, not a scaffolding gap that was subsequently fixed) — no GUID amendment, no `getMetricHistory` accessor, no regeneration.
